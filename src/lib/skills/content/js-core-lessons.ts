// JavaScript Core Lessons — J0 through J26
// Following Knowledge Map v3 and Stage 3 Lesson Blueprint

export const jsCoreLessons = [
  // ============================================
  // J0 — DevTools
  // ============================================
  {
    slug: "devtools",
    track: "js-core",
    order: 1,
    title: "DevTools — инструменты разработчика",
    summary: "Научиться открывать и использовать DevTools браузера: консоль, вкладка Elements, Network, чтобы видеть код, ошибки и то, как работает страница.",
    level: "Foundation",
    prerequisites: [],
    learningObjective: "После этого урока вы сможете открывать DevTools, читать сообщения об ошибках в консоли, осматривать HTML-элементы и понимать, почему DevTools необходимы для изучения JavaScript.",
    shortExplanation: "DevTools — это встроенные инструменты каждого современного браузера. Они позволяют видеть код страницы, запускать JavaScript, находить ошибки и понимать, как браузер работает. DevTools — ваш главный друг при изучении JavaScript.",
    detailedExplanation: "DevTools (Developer Tools) — это набор инструментов, который есть в каждом современном браузере: Chrome, Firefox, Safari, Edge. Они позволяют заглянуть внутрь веб-страницы и увидеть, что происходит «под капотом».\n\nЗачем это нужно? Потому что JavaScript работает в браузере. Чтобы понять, что делает ваш код, где ошибка и как страница устроена, нужно использовать DevTools. Это как рентген для веб-страницы.\n\nКак открыть DevTools:\n- F12 (или Ctrl+Shift+I на Windows/Linux, Cmd+Option+I на Mac)\n- Правой кнопкой мыши на странице → «Просмотреть код» (Inspect)\n- Ctrl+Shift+J (откроет сразу консоль)",
    mentalModel: "DevTools — это как «рентген» для веб-страницы. Вы видите не готовую картинку, а то, из чего она состоит: HTML-структуру, CSS-стили, JavaScript-код и ошибки.",
    examples: [
      {
        level: "minimal",
        code: "// Откройте DevTools (F12) и вставьте в консоль:\nconsole.log('Привет!');\n// В консоли появится: Привет!",
        explanation: "Это самый первый шаг: открыть консоль и увидеть, что JavaScript выполняет ваши команды."
      },
      {
        level: "simple",
        code: "// Попробуйте:\nconsole.log(2 + 2);    // 4\nconsole.log('Hello' + ' ' + 'World'); // Hello World\nconsole.log(true);     // true",
        explanation: "Консоль выполняет JavaScript-код и показывает результат. Это удобно для быстрой проверки идей."
      },
      {
        level: "real",
        code: "// Ошибка в коде — посмотрите, как DevTools помогает её найти:\nconsole.log('Начало');\nconsole.log(undefinedVariable); // ReferenceError!\nconsole.log('Конец'); // Эта строка НЕ выполнится",
        explanation: "Когда происходит ошибка, DevTools покажет её в консоли: тип ошибки, номер строки и описание. Это поможет вам найти и исправить проблему."
      }
    ],
    commonMistakes: [
      {
        wrong: "Игнорировать ошибки в консоли",
        why: "Ошибка в консоли — это сообщение от браузера о проблеме. Если её не читать, невозможно понять, что пошло не так.",
        right: "Всегда смотрите в консоль первым делом, когда код не работает."
      },
      {
        wrong: "Использовать alert() для отладки",
        why: "alert() блокирует всю страницу и показывает только текст. Консоль — в million раз удобнее.",
        right: "Используйте console.log(), console.table(), console.error() — они показывают структурированную информацию."
      }
    ],
    importantToRemember: [
      "DevTools открываются через F12 или Ctrl+Shift+I",
      "Консоль — основной инструмент для отладки JavaScript",
      "Ошибки в консоли содержат номер строки и описание проблемы",
      "Elements показывает HTML-структуру страницы",
      "Network показывает, какие файлы загружает браузер"
    ],
    connection: {
      back: "У этого урока нет предварительных требований — это отправная точка JavaScript.",
      forward: "В следующем уроке (J1) вы напишете свой первый JavaScript-код и поймёте, как работает язык."
    }
  },

  // ============================================
  // J1 — Introduction to JS
  // ============================================
  {
    slug: "intro-to-js",
    track: "js-core",
    order: 2,
    title: "Что такое JavaScript",
    summary: "Понять, что JavaScript — это язык программирования, который делает веб-страницы интерактивными, и как он выполняется в браузере.",
    level: "Beginner",
    prerequisites: ["devtools"],
    learningObjective: "После этого урока вы сможете объяснить, что такое JavaScript, как он связан с HTML, и запустить первую программу через console.log().",
    shortExplanation: "JavaScript — это язык программирования, который браузер понимает и выполняет. Он делает страницы интерактивными: реагирует на клики, отправляет данные, меняет содержимое. HTML задаёт структуру, CSS — внешний вид, а JavaScript — поведение.",
    detailedExplanation: "HTML создаёт скелет страницы: заголовки, параграфы, кнопки. CSS покрашивает этот скелет: цвета, шрифты, расположение. Но без JavaScript страница статична — ничего не происходит, когда вы нажимаете кнопку.\n\nJavaScript добавляет поведение. Когда вы нажимаете кнопку и появляется выпадающее меню — это JavaScript. Когда форма проверяет, заполнены ли поля — это JavaScript. Когда корзина покупок обновляется — это тоже JavaScript.\n\nКак JavaScript попадает в браузер:\n1. Браузер скачивает HTML-страницу\n2. Находит тег <script> — это указание: «здесь есть JavaScript»\n3. Скачивает и выполняет JavaScript-код\n4. Код работает с HTML-элементами и реагирует на действия пользователя",
    mentalModel: "Представьте дом: HTML — это стены и перегородки, CSS — покраска и мебель, JavaScript — это электричество и кнопки, которые делают дом удобным для жизни.",
    examples: [
      {
        level: "minimal",
        code: "console.log('Привет, мир!');\n// Выведет в консоль: Привет, мир!",
        explanation: "console.log() — это функция, которая отправляет текст в консоль браузера. Это самый простой способ убедиться, что JavaScript работает."
      },
      {
        level: "simple",
        code: "let name = 'Анна';\nlet age = 25;\nconsole.log('Меня зовут ' + name + ', мне ' + age + ' лет');\n// Выведет: Меня зовут Анна, мне 25 лет",
        explanation: "JavaScript умеет работать с данными: сохранять их в переменные и комбинировать."
      },
      {
        level: "real",
        code: "// Интерактивность — реакция на действие пользователя:\n// В HTML: <button onclick=\"alert('Привет!')\">Нажми меня</button>\n// Когда вы кликаете на кнопку — появляется всплывающее окно.\n// Это JavaScript реагирует на событие клика.",
        explanation: "Вот зачем нужен JavaScript: страница перестаёт быть статичной и начинает реагировать на пользователя."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что JavaScript — это Java",
        why: "Названия похожи, но это совершенно разные языки. JavaScript появился за 3 года до Java. Название — маркетинговый ход 1995 года.",
        right: "JavaScript и Java — разные языки. Не путайте их."
      },
      {
        wrong: "Путать порядок: CSS перед HTML",
        why: "HTML создаёт структуру, CSS стилизует её. Без HTML нет чего стилизовать.",
        right: "Порядок: HTML (структура) → CSS (стили) → JavaScript (поведение)."
      }
    ],
    importantToRemember: [
      "JavaScript — язык программирования для браузера",
      "HTML задаёт структуру, CSS — внешний вид, JavaScript — поведение",
      "console.log() — основной инструмент для вывода информации",
      "JavaScript выполняется в движке браузера (V8 в Chrome)",
      "Код выполняется сверху вниз, строка за строкой"
    ],
    sources: [
      { title: "MDN: JavaScript", url: "https://developer.mozilla.org/ru/docs/Web/JavaScript" },
      { title: "ECMAScript: спецификация языка", url: "https://tc39.es/ecma262/" }
    ],
    connection: {
      back: "Вы научились пользоваться DevTools (J0) — теперь вы будете писать код, который появляется в консоли.",
      forward: "Следующий урок (J2) знакомит с переменными — как хранить данные в JavaScript."
    }
  },

  // ============================================
  // J2 — Variables and Values
  // ============================================
  {
    slug: "variables-and-values",
    track: "js-core",
    order: 3,
    title: "Переменные и значения",
    summary: "Научиться сохранять данные в переменные с помощью let и const, понимая разницу между ними и правила именования.",
    level: "Beginner",
    prerequisites: ["intro-to-js"],
    learningObjective: "После этого урока вы сможете создавать переменные через let и const, объяснять разницу между ними и правильно называть переменные.",
    shortExplanation: "Переменная — это именованная «коробка» для хранения данных. Вы даёте коробке имя и кладёте внутрь значение. const запрещает переприсваивание имени — нельзя положить другое значение или ссылку (если значение — объект или массив, его внутреннее содержимое менять можно — об этом позже). let позволяет менять содержимое.",
    detailedExplanation: "Представьте, что у вас есть множество коробок. На каждой коробке — наклейка с именем (это имя переменной), а внутри — что-то (это значение).\n\nlet age = 25;\n// Коробка с наклейкой «age», внутри лежит число 25\n\nJavaScript предоставляет три способа объявить переменную:\n\n1. const — «константа»: переприсвоить имя нельзя — нельзя положить другую ссылку/значение. По умолчанию для всего. (Если значение — объект или массив, его внутреннее содержимое всё ещё можно менять — подробнее в уроке об объектах.)\n\n2. let — «переменная»: содержимое можно менять. Для счётчиков, флагов, промежуточных значений.\n\n3. var — «старый способ»: работает, но имеет подводные камни с областью видимости. Почти никогда не используется в современном коде.\n\nПравило: начинайте с const. Если понадобится менять — переключайтесь на let. Забудьте о var до тех пор, пока не столкнётесь с ним в чужом коде.\n\nИменование переменных:\n- camelCase — рекомендуемый стиль: userName, maxRetries, isActive\n- Булевы переменные начинаются с is/has/can\n- Массивы — во множественном числе: items\n- Нельзя начинать имя с цифры, использовать дефис или зарезервированные слова",
    mentalModel: "Переменная — это коробка с наклейкой. Наклейка — это имя (identifier). Содержимое — это значение (value). const = коробка с замком (нельзя положить другое). let = коробка без замка (можно менять содержимое). Модель коробки помогает начать — позже, в уроке об объектах, вы увидите, что const фиксирует саму привязку имени, а не содержимое: свойства объекта, созданного через const, менять можно.",
    examples: [
      {
        level: "minimal",
        code: "const name = 'Анна';\nlet age = 25;\nconsole.log(name); // Анна\nconsole.log(age);  // 25",
        explanation: "Создаём две переменные: const для имени (не будет меняться), let для возраста (может измениться)."
      },
      {
        level: "simple",
        code: "let temperature = 20;\nconsole.log(temperature); // 20\n\ntemperature = 25;\nconsole.log(temperature); // 25\n\n// const MAX = 100;\n// MAX = 200; // Ошибка! TypeError",
        explanation: "let позволяет менять значение, const — нет. Если попробовать переприсвоить const, JavaScript выбросит ошибку."
      },
      {
        level: "real",
        code: "const PI = 3.14159;\nlet radius = 5;\nlet area = PI * radius * radius;\n\nconsole.log(`Площадь круга: ${area}`); // 78.53975",
        explanation: "PI не меняется (это математическая константа), radius может меняться (размер круга), area вычисляется из них."
      }
    ],
    commonMistakes: [
      {
        wrong: "const означает «неизменяемое значение»",
        why: "const запрещает переприсваивание, но объекты и массивы внутри могут меняться (push, изменение свойств).",
        right: "const запрещает переприсваивание binding. Объект/массив внутри может быть изменён."
      },
      {
        wrong: "Использование переменной до объявления",
        why: "JavaScript выполняет код сверху вниз. Переменная доступна только после строки с объявлением.",
        right: "Объявляйте переменные перед использованием."
      },
      {
        wrong: "Именование переменных через дефис или пробел",
        why: "JavaScript не понимает my-name как имя переменной — он видит минус и пытается вычесть.",
        right: "Используйте camelCase: myName, а не my-name."
      }
    ],
    importantToRemember: [
      "Начинайте с const, переключайтесь на let если нужно менять",
      "var — устарел, избегайте его в новом коде",
      "camelCase — рекомендуемый стиль именования",
      "const не делает значение неизменяемым — запрещает только переприсваивание",
      "Переменные доступны только после объявления"
    ],
    connection: {
      back: "Вы узнали, что такое JavaScript (J1), — теперь вы умеете хранить данные.",
      forward: "Следующий урок (J3) разбирает, какие типы значений можно хранить: числа, строки, булевы значения и другие."
    }
  },

  // ============================================
  // J3 — Data Types
  // ============================================
  {
    slug: "data-types",
    track: "js-core",
    order: 4,
    title: "Типы данных",
    summary: "Понять, какие виды значений существуют в JavaScript: примитивы (number, string, boolean, undefined, null) и объекты, и как определить тип с помощью typeof.",
    level: "Beginner",
    prerequisites: ["variables-and-values"],
    learningObjective: "После этого урока вы сможете назвать базовые типы JavaScript, использовать typeof для проверки типа значения и объяснять разницу между примитивами и объектами.",
    shortExplanation: "JavaScript различает несколько типов значений: числа, строки, логические значения, undefined и null — это примитивы. Объекты (включая массивы и функции) — отдельная категория. typeof помогает определить тип значения.",
    detailedExplanation: "Типы данных — это категории значений. Каждое значение в JavaScript принадлежит к определённому типу, и это определяет, что с ним можно делать.\n\nПримитивные типы (primitives):\n1. number — числа: 42, 3.14, -10, Infinity, NaN\n2. string — строки (текст): 'привет', \"мир\", `шаблон`\n3. boolean — логические: true или false\n4. undefined — значение «не задано» (переменная создана, но значение не присвоено)\n5. null — значение «пусто» (намеренно)\n6. bigint — большие числа: 9007199254740991n\n7. symbol — уникальные идентификаторы: Symbol('id')\n\nОбъекты (objects):\n- { name: 'Анна', age: 25 } — объект\n- [1, 2, 3] — массив (это объект)\n- function() {} — функция (это объект)\n\nРазница между примитивами и объектами:\n- Примитивы — неизменяемы (immutable). Вы не можете изменить строку 'hello' — только создать новую.\n- Объекты — изменяемы (mutable). Можно менять их свойства.\n- Примитивы сравниваются по значению, объекты — по ссылке.\n\ntypeof — оператор для определения типа:\ntypeof 42 → 'number'\ntypeof 'hello' → 'string'\ntypeof true → 'boolean'\ntypeof undefined → 'undefined'\ntypeof null → 'object' (это историческая ошибка в JavaScript)\ntypeof {} → 'object'\ntypeof [] → 'object'",
    mentalModel: "Типы данных — это как категории в каталоге: числа в одном разделе, тексты в другом, логические значения — в третьем. typeof — это менеджер, который говорит, к какой категории относится вещь.",
    examples: [
      {
        level: "minimal",
        code: "let age = 25;          // number\nlet name = 'Анна';     // string\nlet isStudent = true;  // boolean\nlet nothing = undefined; // undefined\n\nconsole.log(typeof age);      // 'number'\nconsole.log(typeof name);     // 'string'\nconsole.log(typeof isStudent); // 'boolean'\nconsole.log(typeof nothing);  // 'undefined'",
        explanation: "typeof возвращает строку с именем типа. Это помогает понять, с чем вы работаете."
      },
      {
        level: "simple",
        code: "let a = 42;\nlet b = '42';\n\nconsole.log(a == b);  // true (приведение типов!)\nconsole.log(a === b); // false (разные типы: number vs string)\n\n// Вот почему === важнее, чем ==",
        explanation: "Одинаковые символы, но разные типы. === сравнивает и значение, и тип. == сравнивает только значение, приводя типы."
      },
      {
        level: "real",
        code: "let user = { name: 'Анна', age: 25 };\nlet numbers = [1, 2, 3];\n\nconsole.log(typeof user); // 'object'\nconsole.log(typeof numbers); // 'object'\nconsole.log(Array.isArray(numbers)); // true\n\n// typeof не различает объекты и массивы!\n// Используйте Array.isArray() для проверки массивов.",
        explanation: "typeof показывает 'object' для объектов и массивов. Чтобы отличить массив от объекта, используйте Array.isArray()."
      }
    ],
    commonMistakes: [
      {
        wrong: "typeof null === 'object'",
        why: "Это историческая ошибка в JavaScript. typeof null возвращает 'object' с 1995 года. Нельзя исправить, сломает обратную совместимость.",
        right: "Запомните: typeof null — это 'object'. Для проверки на null используйте === null."
      },
      {
        wrong: "Думать, что string — это примитив, который можно изменить",
        why: "Строки в JavaScript неизменяемы. Вы не можете изменить отдельный символ строки.",
        right: "Строки immutable. Любая операция создаёт новую строку: 'hello'.toUpperCase() → 'HELLO' (оригинал не меняется)."
      },
      {
        wrong: "Путать undefined и null",
        why: "undefined — это «значение не задано» (автоматически). null — это «значение пусто» (намеренно).",
        right: "undefined = значение не задано. null = значение пусто (намеренно)."
      }
    ],
    importantToRemember: [
      "Примитивы: number, string, boolean, undefined, null, bigint, symbol",
      "Объекты: {}, [], function — всё, что не примитив",
      "typeof помогает определить тип значения",
      "typeof null === 'object' — историческая ошибка",
      "Примитивы сравниваются по значению, объекты — по ссылке"
    ],
    connection: {
      back: "Вы умеете создавать переменные (J2) — теперь вы знаете, какие значения они могут хранить.",
      forward: "Следующий урок (J4) — операторы: как работать со значениями: вычисления, сравнения и логические операции."
    }
  },

  // ============================================
  // J4 — Operators + ??
  // ============================================
  {
    slug: "operators",
    track: "js-core",
    order: 5,
    title: "Операторы",
    summary: "Научиться использовать арифметические, строковые, сравнительные и логические операторы, включая ?? (nullish coalescing).",
    level: "Beginner",
    prerequisites: ["data-types"],
    learningObjective: "После этого урока вы сможете использовать арифметические, сравнительные, логические операторы и оператор ??, а также объяснять разницу между == и ===.",
    shortExplanation: "Операторы — это символы, которые выполняют действия со значениями: сложение (+), сравнение (===), логические операции (&&, ||) и особый оператор ?? (nullish coalescing), который выбирает значение, если оно null или undefined.",
    detailedExplanation: "Операторы делятся на категории:\n\nАрифметические: + - * / % (остаток) ** (степень)\n\nСтроковые:\n- + конкатенирует строки: 'hello' + ' ' + 'world'\n\nСравнительные:\n- === строгое сравнение (значение И тип)\n- == нестрогое сравнение (только значение, с приведением типов)\n- > < >= <=\n\nЛогические:\n- && И (оба должны быть true)\n- || ИЛИ (хотя бы одно true)\n- ! НЕ (инвертирует)\n\nNullish coalescing (??):\n- Возвращает правое значение, если левое — null или undefined\n- Отличие от ||: || считает 0, '' и false «пустыми», ?? — нет\n\nПрисваивающие:\n+= -= *= /= %= ++ --\n\nОсобенности:\n- typeof — определяет тип значения\n- in — проверяет наличие свойства в объекте\n- instanceof — проверяет прототип объекта\n\nПриоритет операторов:\n1. () (скобки)\n2. ** (степень)\n3. * / %\n4. + -\n5. === !== > < >= <=\n6. ! && ||\n7. ??\n8. = += -= и т.д.",
    mentalModel: "Операторы — это как арифметика в школе, только расширенная. + - * / — это база. === — это вопрос «это одинаковые вещи?». && — это «оба условия должны быть выполнены».",
    examples: [
      {
        level: "minimal",
        code: "let a = 10;\nlet b = 3;\n\nconsole.log(a + b);  // 13\nconsole.log(a - b);  // 7\nconsole.log(a * b);  // 30\nconsole.log(a / b);  // 3.333...\nconsole.log(a % b);  // 1 (остаток от деления)",
        explanation: "Базовые арифметические операторы работают как в математике."
      },
      {
        level: "simple",
        code: "let input = null;\nlet defaultName = 'Гость';\n\n// || считает 0, '' и false «пустыми»\nlet name1 = input || defaultName;  // 'Гость'\n\n// ?? считает пустыми только null/undefined\nlet name2 = input ?? defaultName;  // 'Гость'\n\nlet count = 0;\nconsole.log(count || 10);  // 10 (0 считается «пустым»!)\nconsole.log(count ?? 10);  // 0 (0 — не null/undefined)",
        explanation: "?? и || ведут себя по-разному с 0, '' и false. ?? более предсказуем для значений по умолчанию."
      },
      {
        level: "real",
        code: "let age = 17;\nlet hasTicket = true;\n\n// && — оба условия должны быть true\nif (age >= 18 && hasTicket) {\n  console.log('Добро пожаловать!');\n} else {\n  console.log('Доступ запрещён');\n}\n\n// || — хотя бы одно true\nlet memberType = 'premium' || 'free'; // 'premium'\nlet discount = 0;\nlet finalPrice = discount || 100; // 100 (0 считается «пустым»!)",
        explanation: "&& и || — основа условной логики. Они позволяют комбинировать несколько условий в одно."
      }
    ],
    commonMistakes: [
      {
        wrong: "Использовать == вместо ===",
        why: "== приводит типы: '5' == 5 → true. Это может привести к неожиданным результатам.",
        right: "Используйте === как стандарт. == изучите, чтобы понимать существующий код."
      },
      {
        wrong: "Путать || и ??",
        why: "|| считает 0, '' и false «пустыми». ?? считает пустыми только null и undefined.",
        right: "Используйте ?? когда хотите значение по умолчанию для null/undefined. Используйте || когда хотите значение по умолчанию для «пустых» значений."
      },
      {
        wrong: "Забывать, что + для строк — конкатенация",
        why: "'5' + 3 → '53' (строка), но '5' - 3 → 2 (число). JavaScriptCastException.",
        right: "Проверяйте типы операндов. Если хотя бы один — строка, + будет конкатенацией."
      }
    ],
    importantToRemember: [
      "=== сравнивает значение И тип, == — только значение",
      "?? возвращает правое значение, если левое null/undefined",
      "|| считает 0, '' и false «пустыми», ?? — нет",
      "&& — оба true, || — хотя бы одно true",
      "Приоритет: () > ** > * / % > + - > === > ! > && || > ?? > ="
    ],
    connection: {
      back: "Вы знаете базовые типы данных (J3) — теперь вы умеете с ними работать.",
      forward: "Следующий урок (J5) — структура кода: инструкции, точки с запятой и комментарии."
    }
  },

  // ============================================
  // J5 — Code Structure and Comments
  // ============================================
  {
    slug: "code-structure",
    track: "js-core",
    order: 6,
    title: "Структура кода и комментарии",
    summary: "Понять, как устроен JavaScript-код: инструкции, точки с запятой, фигурные скобки и комментарии для пояснения кода.",
    level: "Beginner",
    prerequisites: ["operators"],
    learningObjective: "После этого урока вы сможете писать правильно оформленный JavaScript-код с корректными точками с запятой, блоками и осмысленными комментариями.",
    shortExplanation: "JavaScript-код состоит из инструкций, которые выполняются сверху вниз. Инструкции заканчиваются точкой с запятой (;). Блоки кода заключаются в фигурные скобки {}. Комментарии начинаются с // (однострочные) или /* ... */ (многострочные).",
    detailedExplanation: "Инструкции (statements) — это команды JavaScript:\nlet x = 10; // инструкция объявления\nconsole.log(x); // инструкция вызова\n\nТочки с запятой:\nJavaScript может работать без точек с запятой (ASI — Automatic Semicolon Insertion), но это может приводить к неожиданным ошибкам. Рекомендуется всегда ставить ;.\n\nФигурные скобки {}:\nОпределяют блоки кода: функции, условия, циклы. Внутри скобок — несколько инструкций.\n\nКомментарии:\n// однострочный комментарий\n/* многострочный\n   комментарий */\n\nКомментарии не выполняются JavaScript — они только для людей. Используйте их, чтобы объяснить ЗАЧЁМ написан сложный код.\n\nПробелы и переносы строк:\nJavaScript игнорирует лишние пробелы и переносы. Код\nlet x = 10;\nи\nlet    x    =    10;\nработают одинаково. Но правильные отступы делают код читаемым.",
    mentalModel: "Код — это рецепт. Инструкции — шаги рецепта. Скобки — блоки шагов (например, «сделать тесто»). Комментарии — пояснения шеф-повара: «именно поэтому добавляем соль сейчас, а не позже».",
    examples: [
      {
        level: "minimal",
        code: "// Это комментарий — JavaScript его игнорирует\nlet name = 'Анна'; // Комментарий в конце строки\n\nconsole.log(name);",
        explanation: "Комментарии начинаются с //. Они не влияют на работу кода, но помогают понять, что происходит."
      },
      {
        level: "simple",
        code: "/* Многострочный комментарий\n   используется для длинных пояснений */\n\nif (age >= 18) {\n  console.log('Добро пожаловать');\n} else {\n  console.log('Вам нет 18');\n}",
        explanation: "/* ... */ — многострочный комментарий. { } — блок кода для условия."
      },
      {
        level: "real",
        code: "// Плохо:\nlet x = 10; let y = 20; let z = x + y;\n\n// Хорошо:\nlet firstNumber = 10;\nlet secondNumber = 20;\nlet sum = firstNumber + secondNumber;\n// Считаем сумму двух чисел для отображения в интерфейсе",
        explanation: "Читаемые имена переменных + комментарии = код, который понимают другие разработчики (и вы через неделю)."
      }
    ],
    commonMistakes: [
      {
        wrong: "Писать весь код в одну строку",
        why: "Нечитаемый код — источник ошибок. Другие (и вы) не смогут его понять.",
        right: "Каждая инструкция — на новой строке. Используйте отступы (2 или 4 пробела)."
      },
      {
        wrong: "Комментировать очевидное",
        why: "// Инкрементируем счётчик (счётчик += 1) — это не помогает, а загромождает.",
        right: "Комментируйте ЗАЧЁМ, а не ЧТО. Если код понятен из имён переменных — комментарий не нужен."
      },
      {
        wrong: "Забывать точки с запятой",
        why: "Без ; JavaScript может неправильно понять границы инструкций.",
        right: "Всегда ставьте ; в конце инструкций."
      }
    ],
    importantToRemember: [
      "// — однострочный комментарий",
      "/* ... */ — многострочный комментарий",
      "Код выполняется сверху вниз",
      "Комментируйте ЗАЧЁМ, а не ЧТО",
      "Используйте отступы для читаемости"
    ],
    connection: {
      back: "Вы знаете операторы (J4) — теперь вы умеете правильно структурировать код.",
      forward: "Следующий урок (J6) — взаимодействие с браузером: alert(), prompt() и confirm()."
    }
  },

  // ============================================
  // J6 — alert/prompt/confirm
  // ============================================
  {
    slug: "alert-prompt-confirm",
    track: "js-core",
    order: 7,
    title: "alert, prompt, confirm",
    summary: "Научиться использовать встроенные функции браузера для взаимодействия с пользователем: показать сообщение, получить ввод, задать вопрос.",
    level: "Beginner",
    prerequisites: ["code-structure"],
    learningObjective: "После этого урока вы сможете использовать alert(), prompt() и confirm() для взаимодействия с пользователем и понимать, когда каждая функция уместна.",
    shortExplanation: "alert() показывает всплывающее окно с сообщением. prompt() запрашивает текст у пользователя. confirm() задаёт вопрос с кнопками OK/Отмена. Это простейший способ взаимодействия с пользователем.",
    detailedExplanation: "JavaScript имеет три встроенные функции для простого взаимодействия:\n\nalert(message) — показывает окно с текстом и кнопкой OK. Блокирует страницу до нажатия.\n\nprompt(message, defaultValue) — показывает окно с полем ввода. Возвращает текст, который ввёл пользователь, или null при отмене.\n\nconfirm(message) — показывает окно с вопросом и кнопками OK/Отмена. Возвращает true или false.\n\nВажно: все три функции блокируют выполнение кода. Пока пользователь не закроет окно, следующая строка не выполнится.\n\nВ современном JavaScript эти функции используются редко — вместо них создают кастомные модальные окна. Но для обучения и быстрой проверки идей они удобны.",
    mentalModel: "alert — как крикнуть «Эй!». prompt — как спросить «Как тебя зовут?». confirm — как задать вопрос «Ты уверен?» с кнопками Да/Нет.",
    examples: [
      {
        level: "minimal",
        code: "alert('Привет!');\n// Покажет окно с текстом «Привет!» и кнопкой OK",
        explanation: "alert() — самый простой способ показать сообщение пользователю."
      },
      {
        level: "simple",
        code: "let name = prompt('Как тебя зовут?', 'Гость');\nconsole.log('Привет, ' + name + '!');\n\n// Если пользователь введёт «Анна» — выведет: Привет, Анна!\n// Если нажмёт Отмену — name будет null",
        explanation: "prompt() возвращает то, что ввёл пользователь. Второй аргумент — значение по умолчанию."
      },
      {
        level: "real",
        code: "let confirmed = confirm('Вы хотите удалить файл?');\n\nif (confirmed) {\n  console.log('Файл удалён');\n} else {\n  console.log('Удаление отменено');\n}",
        explanation: "confirm() возвращает true при OK и false при Отмене. Это удобно для подтверждения опасных действий."
      }
    ],
    commonMistakes: [
      {
        wrong: "Использовать alert() в продакшен-коде",
        why: "alert() блокирует всю страницу и выглядит непрофессионально. Пользователи ненавидят всплывающие окна.",
        right: "alert/prompt/confirm — для обучения и быстрой отладки. В продакшене — кастомные модальные окна."
      },
      {
        wrong: "Забывать, что prompt() может вернуть null",
        why: "Если пользователь нажмёт Отмену, prompt() вернёт null, а не пустую строку.",
        right: "Всегда проверяйте: if (name !== null) { ... }"
      },
      {
        wrong: "Думать, что alert() что-то возвращает",
        why: "alert() всегда возвращает undefined. Он ничего не возвращает — только показывает сообщение.",
        right: "alert() — для вывода. prompt() — для ввода. confirm() — для подтверждения."
      }
    ],
    importantToRemember: [
      "alert() показывает сообщение, prompt() запрашивает ввод, confirm() задаёт вопрос",
      "Все три блокируют выполнение кода",
      "prompt() возвращает null при отмене",
      "alert() возвращает undefined (ничего не возвращает)",
      "В продакшене используйте кастомные модальные окна"
    ],
    connection: {
      back: "Вы умеете структурировать код (J5) — теперь вы можете взаимодействовать с пользователем.",
      forward: "Следующий урок (J7) — условия: как код принимает решения."
    }
  },

  // ============================================
  // J7 — Conditions
  // ============================================
  {
    slug: "conditions",
    track: "js-core",
    order: 8,
    title: "Условия",
    summary: "Научиться принимать решения в коде с помощью if/else, switch и тернарного оператора, понимая truthy и falsy значения.",
    level: "Beginner",
    prerequisites: ["alert-prompt-confirm", "operators"],
    learningObjective: "После этого урока вы сможете писать условную логику через if/else, switch и тернарный оператор, а также объяснять значения truthy и falsy.",
    shortExplanation: "Условия позволяют коду принимать решения: если условие истинно — выполнить один блок кода, если нет — другой. if/else — основной способ. switch — для множества вариантов. Тернарный оператор — для коротких условий в одну строку.",
    detailedExplanation: "if/else — основной способ условий:\nif (условие) {\n  // выполняется, если условие true\n} else {\n  // выполняется, если условие false\n}\n\nМожно добавить else if для дополнительных вариантов:\nif (temp < 0) {\n  // мороз\n} else if (temp < 20) {\n  // прохладно\n} else {\n  // тепло\n}\n\nswitch — для множества вариантов:\nswitch (значение) {\n  case 'option1': ...\n  case 'option2': ...\n  default: ...\n}\n\nТернарный оператор — сокращённая запись if/else:\nlet result = условие ? значение_если_true : значение_если_false;\n\nTruthy и falsy:\nJavaScript может преобразовать любое значение в boolean.\nFalsy значения (считаются false):\n- false, 0, '', null, undefined, NaN\n\nTruthy значения (считаются true):\n- Всё остальное: true, 1, 'hello', [], {},\n  function() {}\n\nЭто важно понимать, потому что JavaScript автоматически преобразует значения в boolean в условиях.",
    mentalModel: "Условия — как развилка на дороге. Если условие true — едем налево, если false — направо. Truthy/falsy — это способ JavaScript определить, есть ли «кто-то на дороге» (даже если это не совсем true/false).",
    examples: [
      {
        level: "minimal",
        code: "let age = 17;\n\nif (age >= 18) {\n  console.log('Добро пожаловать');\n} else {\n  console.log('Вам нет 18');\n}\n// Выведет: Вам нет 18",
        explanation: "Базовое условие: если age >= 18 — одно, иначе — другое."
      },
      {
        level: "simple",
        code: "let score = 85;\n\nlet grade = score >= 90 ? 'A' :\n            score >= 80 ? 'B' :\n            score >= 70 ? 'C' : 'F';\n\nconsole.log(grade); // 'B'",
        explanation: "Тернарный оператор можно вкладывать друг в друга для множества вариантов."
      },
      {
        level: "real",
        code: "let username = '';\n\n// Truthy/falsy в действии:\nif (username) {\n  console.log('Привет, ' + username);\n} else {\n  console.log('Привет, гость!');\n}\n// Выведет: Привет, гость! (пустая строка — falsy)",
        explanation: "JavaScript автоматически преобразует пустую строку в false. Это удобно для проверки на «пустоту»."
      }
    ],
    commonMistakes: [
      {
        wrong: "Забывать break в switch",
        why: "Без break JavaScript «проваливается» в следующий case и выполняет его тоже (fall-through).",
        right: "Всегда добавляйте break после каждого case. Или используйте return."
      },
      {
        wrong: "Путать = и ==",
        why: "= — присваивание, == — сравнение. if (x = 5) всегда true!",
        right: "if (x === 5) — сравнение. if (x = 5) — присваивание (и всегда true)."
      },
      {
        wrong: "Использовать if (x == null) для проверки на undefined",
        why: "== null проверяет и null, и undefined. Это может быть неожиданным.",
        right: "Явно проверяйте: if (x === null || x === undefined)"
      }
    ],
    importantToRemember: [
      "if/else — основной способ условий",
      "switch — для множества вариантов (не забывайте break!)",
      "Тернарный оператор ? : — для коротких условий",
      "Falsy: false, 0, '', null, undefined, NaN",
      "Всё остальное — truthy (включая '0', [], {})"
    ],
    connection: {
      back: "Вы знаете операторы (J4) — теперь вы умеете принимать решения с их помощью.",
      forward: "Следующий урок (J8) — циклы: как повторять действия."
    }
  },

  // ============================================
  // J8 — Loops
  // ============================================
  {
    slug: "loops",
    track: "js-core",
    order: 9,
    title: "Циклы",
    summary: "Научиться повторять действия с помощью циклов for, while, do...while и for...of, понимая, когда использовать каждый.",
    level: "Beginner",
    prerequisites: ["conditions"],
    learningObjective: "После этого урока вы сможете писать циклы for, while, do...while и for...of и выбирать подходящий цикл для каждой ситуации.",
    shortExplanation: "Циклы позволяют выполнять один и тот же код несколько раз. for — когда известно количество итераций. while — когда условие выхода. for...of — для перебора массивов. break — выход из цикла, continue — пропуск итерации.",
    detailedExplanation: "for — основной цикл с счётчиком:\nfor (let i = 0; i < 5; i++) {\n  console.log(i); // 0, 1, 2, 3, 4\n}\n\nТри части:\n1. let i = 0 — начальное значение (выполняется один раз)\n2. i < 5 — условие (проверяется перед каждой итерацией)\n3. i++ — инкремент (выполняется после каждой итерации)\n\nwhile — цикл с условием:\nwhile (условие) {\n  // тело цикла\n}\n\ndo...while — цикл, который выполняется хотя бы раз:\ndo {\n  // тело цикла\n} while (условие);\n\nfor...of — перебор массивов:\nlet fruits = ['яблоко', 'банан', 'вишня'];\nfor (let fruit of fruits) {\n  console.log(fruit);\n}\n\nbreak — немедленный выход из цикла.\ncontinue — пропуск текущей итерации и переход к следующей.",
    mentalModel: "Цикл — как карусель: вы крутитесь, пока не решите выйти (break) или пока не закончится время (условие). for...of — как проходить по списку покупок: берёте каждый пункт по очереди.",
    examples: [
      {
        level: "minimal",
        code: "for (let i = 0; i < 3; i++) {\n  console.log(i);\n}\n// Выведет: 0, 1, 2",
        explanation: "for — цикл с счётчиком. Начинаем с 0, увеличиваем на 1, пока i < 3."
      },
      {
        level: "simple",
        code: "let fruits = ['яблоко', 'банан', 'вишня'];\n\nfor (let fruit of fruits) {\n  console.log(fruit);\n}\n// Выведет: яблоко, банан, вишня",
        explanation: "for...of — удобный способ перебрать каждый элемент массива."
      },
      {
        level: "real",
        code: "let attempts = 0;\nlet correct = false;\n\nwhile (!correct && attempts < 3) {\n  let answer = prompt('Угадайте число от 1 до 10');\n  if (answer === '7') {\n    correct = true;\n    console.log('Правильно!');\n  } else {\n    attempts++;\n    console.log('Неверно. Осталось попыток: ' + (3 - attempts));\n  }\n}",
        explanation: "while — цикл с условием. Выполняется, пока correct = false и attempts < 3."
      }
    ],
    commonMistakes: [
      {
        wrong: "Бесконечный цикл",
        why: "while (true) { ... } без break — бесконечный цикл. Браузер зависнет.",
        right: "Всегда проверяйте, что условие выхода достижимо."
      },
      {
        wrong: "Изменение массива во время for...of",
        why: "Добавление/удаление элементов во время итерации может привести к пропуску элементов.",
        right: "Не изменяйте массив во время перебора. Используйте filter/map для создания нового массива."
      },
      {
        wrong: "Забывать i++ в цикле for",
        why: "Без i++ счётчик никогда не увеличится → бесконечный цикл.",
        right: "Всегда проверяйте, что счётчик изменяется."
      }
    ],
    importantToRemember: [
      "for — когда известно количество итераций",
      "while — когда условие выхода",
      "for...of — для перебора массивов",
      "break — выход из цикла",
      "continue — пропуск итерации"
    ],
    connection: {
      back: "Вы знаете условия (J7) — теперь вы умеете повторять действия на основе условий.",
      forward: "Следующий урок (J9) — функции: как упаковывать код в переиспользуемые блоки."
    }
  },

  // ============================================
  // J9 — Functions
  // ============================================
  {
    slug: "functions",
    track: "js-core",
    order: 10,
    title: "Функции",
    summary: "Научиться создавать функции для повторного использования кода, понимая параметры, аргументы, return и разницу между declaration и expression.",
    level: "Beginner",
    prerequisites: ["loops"],
    learningObjective: "После этого урока вы сможете создавать функции с параметрами и возвращаемыми значениями, объяснять разницу между function declaration и expression и понимать локальную область видимости.",
    shortExplanation: "Функция — это блок кода, который можно вызывать много раз. Вы определяете функцию один раз и используете сколько угодно раз. Параметры — это переменные функции. Return — это значение, которое функция возвращает.",
    detailedExplanation: "Зачем нужны функции?\n\nБез функций каждый раз, когда нужно выполнить одно и то же действие, пришлось бы копировать код. Функция позволяет записать действие один раз и вызывать его сколько угодно раз.\n\nСинтаксис:\nfunction greet(name) {\n  return 'Привет, ' + name + '!';\n}\n\nlet message = greet('Анна'); // 'Привет, Анна!'\n\nПараметры vs Аргументы:\n- Параметры — имена в определении функции: (name, age)\n- Аргументы — реальные значения при вызове: greet('Анна', 25)\n\nReturn:\n- return возвращает значение из функции\n- Без return функция возвращает undefined\n- return завершает функцию — код после return не выполняется\n\nFunction Declaration vs Expression:\n// Declaration — можно вызывать до определения (hoisting)\nfunction add(a, b) { return a + b; }\n\n// Expression — нельзя вызывать до определения\nconst multiply = function(a, b) { return a * b; }\n\nЛокальные переменные:\nПеременные, объявленные внутри функции, доступны только внутри неё.",
    mentalModel: "Функция — как рецепт. Один раз записали: «возьмите муку, яйца, сахар → получите торт». Теперь можно «вызвать рецепт» сколько угодно раз, меняя только ингредиенты (параметры).",
    examples: [
      {
        level: "minimal",
        code: "function greet(name) {\n  return 'Привет, ' + name + '!';\n}\n\nconsole.log(greet('Анна')); // Привет, Анна!\nconsole.log(greet('Пётр')); // Привет, Пётр!",
        explanation: "Функция принимает имя и возвращает приветствие. Один код — много вызовов."
      },
      {
        level: "simple",
        code: "function add(a, b) {\n  return a + b;\n}\n\nfunction multiply(a, b) {\n  return a * b;\n}\n\nconsole.log(add(2, 3));      // 5\nconsole.log(multiply(2, 3)); // 6",
        explanation: "Функции возвращают результат через return. Можно создавать функции для любых вычислений."
      },
      {
        level: "real",
        code: "function calculateArea(width, height) {\n  if (width <= 0 || height <= 0) {\n    return null; // Ошибка: размеры должны быть положительными\n  }\n  return width * height;\n}\n\nlet area = calculateArea(10, 5);\nif (area !== null) {\n  console.log('Площадь: ' + area); // Площадь: 50\n} else {\n  console.log('Неверные размеры');\n}",
        explanation: "Функция может возвращать null для индикации ошибки — это.common pattern в JavaScript."
      }
    ],
    commonMistakes: [
      {
        wrong: "Забывать return",
        why: "Без return функция возвращает undefined. Частая ошибка: function add(a, b) { a + b; }",
        right: "Всегда проверяйте, что функция возвращает ожидаемое значение."
      },
      {
        wrong: "Использовать console.log вместо return",
        why: "console.log выводит в консоль, но не возвращает значение для дальнейшего использования.",
        right: "return — для возврата значения. console.log — для отладки."
      },
      {
        wrong: "Вызывать функцию до её определения (expression)",
        why: "Function expression (const fn = function() {}) не поднимается (hoisted) вверх.",
        right: "Используйте function declaration, если нужен hoisting."
      }
    ],
    importantToRemember: [
      "Функция — блок кода для повторного использования",
      "Параметры — имена в определении, аргументы — значения при вызове",
      "return возвращает значение и завершает функцию",
      "Без return — undefined",
      "Локальные переменные доступны только внутри функции"
    ],
    sources: [
      { title: "MDN: функции", url: "https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Functions" },
      { title: "Дока: функции", url: "https://doka.guide/js/function/" }
    ],
    connection: {
      back: "Вы знаете циклы (J8) — теперь вы можете упаковывать повторяющуюся логику в переиспользуемые функции.",
      forward: "Следующий урок (J10) — стрелочные функции: более короткий синтаксис создания функций."
    }
  },

  // ============================================
  // J10 — Arrow Functions
  // ============================================
  {
    slug: "arrow-functions",
    track: "js-core",
    order: 11,
    title: "Стрелочные функции",
    summary: "Научиться создавать короткие функции с помощью синтаксиса => и понимать разницу с обычными функциями.",
    level: "Beginner",
    prerequisites: ["functions"],
    learningObjective: "После этого урока вы сможете писать стрелочные функции во всех формах и объяснять, когда их использовать вместо обычных функций.",
    shortExplanation: "Стрелочная функция — это сокращённый способ написания функции. const greet = (name) => 'Привет, ' + name + '!'; — та же функция, но короче. Стрелочные функции особенно удобны для callback-функций.",
    detailedExplanation: "Стрелочные функции (arrow functions) появились в ES6 (2015) и стали стандартом в современном JavaScript.\n\nСинтаксис:\n// Полная форма\nconst add = (a, b) => {\n  return a + b;\n};\n\n// Однострочная (неявный return)\nconst add = (a, b) => a + b;\n\n// Один параметр (скобки необязательны)\nconst double = x => x * 2;\n\n// Нет параметров\nconst getFive = () => 5;\n\nКогда использовать arrow functions:\n- Для callback-функций: [1,2,3].map(x => x * 2)\n- Для коротких функций\n- Когда не нужен свой this (обсудим в Intermediate)\n\nКогда НЕ использовать:\n- Когда нужен вызов функции до её объявления — поднятие (hoisting) есть только у function declaration, а у const/let со стрелочной функцией его нет\n- Когда нужен собственный this (обсудим в Intermediate)\n- Для объектных методов (обсудим в Intermediate)",
    mentalModel: "Стрелочная функция — это «быстрая запись» обычной функции. Как короткая записка вместо длинного письма: то же сообщение, но быстрее писать и читать.",
    examples: [
      {
        level: "minimal",
        code: "const greet = (name) => 'Привет, ' + name + '!';\nconsole.log(greet('Анна')); // Привет, Анна!",
        explanation: "Стрелочная функция с одним параметром и неявным return."
      },
      {
        level: "simple",
        code: "const numbers = [1, 2, 3, 4, 5];\nconst doubled = numbers.map(x => x * 2);\nconsole.log(doubled); // [2, 4, 6, 8, 10]",
        explanation: "Стрелочные функции идеальны для callback-функций: коротко, читаемо, понятно."
      },
      {
        level: "real",
        code: "const calculate = {\n  add: (a, b) => a + b,\n  multiply: (a, b) => a * b\n};\n\nconsole.log(calculate.add(2, 3));      // 5\nconsole.log(calculate.multiply(2, 3)); // 6",
        explanation: "Стрелочные функции удобны для методов объекта (но помните про this — обсудим в Intermediate)."
      }
    ],
    commonMistakes: [
      {
        wrong: "Забывать return в однострочной форме",
        why: "const add = (a, b) => a + b; — OK\nconst add = (a, b) => { a + b; }; — undefined!",
        right: "В однострочной форме return неявный. В блочной {} — явный."
      },
      {
        wrong: "Использовать стрелочные функции везде",
        why: "Стрелочные функции не имеют своего this. А стрелочная функция, сохранённая в const/let, недоступна до строки объявления: поднятия (hoisting), как у function declaration, здесь нет. Это может сломать код.",
        right: "Используйте стрелочные для callback-функций. Обычные (function declaration) — для методов и когда нужен вызов функции до её объявления (hoisting)."
      },
      {
        wrong: "Путать () => {} с объектом {}",
        why: "const fn = () => ({ key: 'value' }); — нужна обёртка () для объекта",
        right: "Для возврата объекта: () => ({ key: 'value' })"
      }
    ],
    importantToRemember: [
      "const fn = (params) => expression — короткая форма",
      "const fn = (params) => { ... } — блочная форма с явным return",
      "Один параметр — скобки необязательны",
      "Нет параметров — нужны пустые скобки ()",
      "Идеальны для callback-функций"
    ],
    connection: {
      back: "Вы знаете обычные функции (J9) — теперь вы знаете более короткий синтаксис стрелок.",
      forward: "Следующий урок (J11) — объекты: как хранить структурированные данные со свойствами."
    }
  },

  // ============================================
  // J11 — Objects
  // ============================================
  {
    slug: "objects",
    track: "js-core",
    order: 12,
    title: "Объекты",
    summary: "Научиться создавать объекты с свойствами, использовать dot и bracket нотацию, понимая методы и ссылочную природу объектов.",
    level: "Beginner",
    prerequisites: ["arrow-functions"],
    learningObjective: "После этого урока вы сможете создавать объекты, читать и изменять свойства, использовать методы и объяснять передачу объектов по ссылке в отличие от значений.",
    shortExplanation: "Объект — это коллекция свойств: ключ/значение. Объекты позволяют группировать связанные данные: { name: 'Анна', age: 25 }. Свойства доступны через точку (obj.key) или скобки (obj['key']). Методы — это функции внутри объекта.",
    detailedExplanation: "Объекты — основная структура данных в JavaScript. Они хранят данные в виде пар «ключ → значение».\n\nСоздание объекта:\nconst user = {\n  name: 'Анна',\n  age: 25,\n  isStudent: true\n};\n\nДоступ к свойствам:\nuser.name      // 'Анна' (dot notation)\nuser['age']    // 25 (bracket notation)\n\nBracket notation нужна:\n- Когда ключ — переменная: user[key]\n- Когда ключ содержит спецсимволы: user['my-key']\n\nИзменение свойств:\nuser.age = 26;\nuser['isStudent'] = false;\n\nДобавление свойств:\nuser.email = 'anna@example.com';\n\nУдаление свойств:\ndelete user.email;\n\nМетоды (функции внутри объекта):\nconst calculator = {\n  add(a, b) { return a + b; },\n  multiply(a, b) { return a * b; }\n};\ncalculator.add(2, 3); // 5\n\nСсылочная природа:\nconst a = { x: 1 };\nconst b = a;    // b ссылается на тот же объект\nb.x = 2;\nconsole.log(a.x); // 2! (а тоже изменилась!)",
    mentalModel: "Объект — как шкаф с ящиками. Каждый ящик имеет наклейку (ключ) и содержимое (значение). Можно открыть ящик по наклейке (dot/bracket notation) или по переменной (только bracket).",
    examples: [
      {
        level: "minimal",
        code: "const person = {\n  name: 'Анна',\n  age: 25\n};\n\nconsole.log(person.name); // 'Анна'\nconsole.log(person.age);  // 25",
        explanation: "Объект хранит связанные данные: имя и возраст — про одного человека."
      },
      {
        level: "simple",
        code: "const user = {\n  name: 'Анна',\n  greet() {\n    return `Привет, меня зовут ${this.name}`;\n  }\n};\n\nconsole.log(user.greet()); // Привет, меня зовут Анна",
        explanation: "Метод — это функция внутри объекта. this ссылается на сам объект."
      },
      {
        level: "real",
        code: "const original = { name: 'Анна', scores: [90, 85, 95] };\nconst copy = original;\n\ncopy.name = 'Пётр';\nconsole.log(original.name); // 'Пётр' (изменился оригинал!)\n\n// Чтобы создать копию:\nconst realCopy = { ...original };\nrealCopy.name = 'Мария';\nconsole.log(original.name); // 'Пётр' (оригинал не изменился)",
        explanation: "Объекты передаются по ссылке. copy — это не копия, а другое имя на тот же объект. Spread {} создаёт поверхностную копию (shallow copy): вложенные объекты и массивы, как scores, остаются общими. Это пример будущей темы: синтаксис spread {...} полностью разбирается в J18."
      }
    ],
    commonMistakes: [
      {
        wrong: "Путать копирование и ссылку",
        why: "const b = a; — не создаёт копию. b и a указывают на тот же объект.",
        right: "Для копии используйте spread: const b = { ...a }"
      },
      {
        wrong: "Использовать dot notation с переменной",
        why: "user.key — ищет свойство с именем 'key', а не значение переменной key.",
        right: "Для переменной используйте bracket notation: user[key]"
      },
      {
        wrong: "Забывать, что [] — массив, {} — объект",
        why: "[1,2,3] — массив (это объект). { a: 1 } — объект. typeof [] === 'object'.",
        right: "Используйте Array.isArray() для проверки на массив."
      }
    ],
    importantToRemember: [
      "Объект — коллекция ключ/значение",
      "Dot notation: obj.key, Bracket: obj['key']",
      "Bracket нужен для переменных и спецсимволов",
      "Методы — функции внутри объекта",
      "Объекты передаются по ссылке, не по значению"
    ],
    sources: [
      { title: "MDN: работа с объектами", url: "https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Working_with_objects" }
    ],
    connection: {
      back: "Вы знаете функции (J10) — теперь вы умеете прикреплять их к объектам как методы.",
      forward: "Следующий урок (J12) — optional chaining: безопасный способ доступа к вложенным свойствам."
    }
  },

  // ============================================
  // J12 — Optional Chaining
  // ============================================
  {
    slug: "optional-chaining",
    track: "js-core",
    order: 13,
    title: "Optional chaining",
    summary: "Научиться безопасно обращаться к вложенным свойствам объектов с помощью ?. , избегая ошибок при отсутствии промежуточных значений.",
    level: "Beginner",
    prerequisites: ["objects"],
    learningObjective: "После этого урока вы сможете использовать optional chaining (?.) для безопасного доступа к вложенным свойствам и методам без ошибок в рантайме.",
    shortExplanation: "Optional chaining (?.) — оператор, который безопасно обращается к свойствам. Если промежуточное значение null или undefined, выражение вернёт undefined вместо ошибки. Раньше приходилось писать длинные проверки.",
    detailedExplanation: "Проблема без optional chaining:\nconst user = {};\nconsole.log(user.address.city); // TypeError: Cannot read property 'city' of undefined\n\nРешение с optional chaining:\nconst user = {};\nconsole.log(user?.address?.city); // undefined (без ошибки!)\n\nСинтаксис:\nobj?.prop          // свойство\nobj?.[key]         // свойство через переменную\nobj?.method()      // метод\n\nКак это работает:\n- Если user — null/undefined → undefined\n- Если user.address — null/undefined → undefined\n- Если user.address.city — существует → возвращает значение\n\nРаньше (без ?.):\nlet city = user && user.address && user.address.city;\n\nТеперь (с ?.):\nlet city = user?.address?.city;\n\nOptional chaining также работает с методами:\nuser.greet?.() // если greet существует — вызовет, нет — undefined",
    mentalModel: "?. — как безопасная лестница. Если на каком-то этаже лестницы нет (null/undefined), вы не упадёте — просто вернётесь в начало (undefined).",
    examples: [
      {
        level: "minimal",
        code: "const user = { name: 'Анна' };\n\n// Без optional chaining:\n// console.log(user.address.city); // Ошибка!\n\n// С optional chaining:\nconsole.log(user?.address?.city); // undefined",
        explanation: "?. проверяет каждое звено цепочки. Если адреса нет — не падает с ошибкой."
      },
      {
        level: "simple",
        code: "const data = {\n  user: {\n    name: 'Анна',\n    posts: [\n      { title: 'Первый пост' }\n    ]\n  }\n};\n\nconsole.log(data?.user?.posts?.[0]?.title); // 'Первый пост'\nconsole.log(data?.user?.posts?.[5]?.title); // undefined",
        explanation: "?. работает с любым количеством вложеностей и с массивами."
      },
      {
        level: "real",
        code: "const config = {\n  settings: {\n    theme: 'dark'\n  }\n};\n\n// Без ?. — длинная проверка:\nlet theme1 = config && config.settings && config.settings.theme;\n\n// С ?. — коротко:\nlet theme2 = config?.settings?.theme;\n\nconsole.log(theme2); // 'dark'",
        explanation: "?. заменяет длинные цепочки && — код становится короче и читаемее."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что ?. предотвращает все ошибки",
        why: "?. защищает только от null/undefined. Другие ошибки (например, вызов не-функции) — нет.",
        right: "?. — это безопасный доступ к свойствам, а не универсальная защита от ошибок."
      },
      {
        wrong: "Использовать ?. для каждого обращения к свойству",
        why: "Если вы уверены, что значение существует — ?. излишен и загромождает код.",
        right: "Используйте ?. когда есть реальная возможность отсутствия значения."
      }
    ],
    importantToRemember: [
      "?. безопасно обращается к свойствам",
      "Если промежуточное значение null/undefined → undefined",
      "?. работает с объектами и массивами",
      "?.method() — безопасный вызов метода",
      "Заменяет длинные цепочки &&"
    ],
    connection: {
      back: "Вы знаете объекты (J11) — теперь вы умеете безопасно обращаться к вложенным свойствам.",
      forward: "Следующий урок (J13) — массивы: упорядоченные коллекции значений."
    }
  },

  // ============================================
  // J13 — Arrays
  // ============================================
  {
    slug: "arrays",
    track: "js-core",
    order: 14,
    title: "Массивы",
    summary: "Научиться создавать массивы, обращаться к элементам по индексу, понимая длину, мутацию и ссылочную природу массивов.",
    level: "Beginner",
    prerequisites: ["optional-chaining"],
    learningObjective: "После этого урока вы сможете создавать массивы, обращаться к элементам по индексу, понимать length и объяснять передачу массивов по ссылке.",
    shortExplanation: "Массив — это упорядоченный список значений. Элементы нумеруются с нуля: [0] — первый элемент. Массивы могут хранить любые значения: числа, строки, объекты, другие массивы.",
    detailedExplanation: "Массив — это особый объект для хранения упорядоченных данных.\n\nСоздание:\nconst numbers = [1, 2, 3, 4, 5];\nconst mixed = [1, 'hello', true, null];\nconst empty = [];\n\nДоступ к элементам (нумерация с 0!):\nnumbers[0]  // 1 (первый элемент)\nnumbers[2]  // 3 (третий элемент)\nnumbers[5]  // undefined (нет элемента)\n\nДлина:\nnumbers.length // 5\n\nИзменение:\nnumbers[0] = 10; // теперь [10, 2, 3, 4, 5]\n\nДобавление:\nnumbers.push(6); // добавить в конец\nnumbers.unshift(0); // добавить в начало\n\nУдаление:\nnumbers.pop(); // удалить с конца\nnumbers.shift(); // удалить с начала\n\nМутация vs Иммутабельность:\nconst arr = [1, 2, 3];\narr.push(4);      // мутация (меняет arr)\nconst arr2 = arr.concat([5]); // иммутабельность (создаёт новый массив)\n\nСсылочная природа:\nconst a = [1, 2, 3];\nconst b = a;\nb.push(4);\nconsole.log(a); // [1, 2, 3, 4] (а тоже изменился!)\n\n// Копия:\nc = [...a]; // spread создаёт копию",
    mentalModel: "Массив — как полка с коробками. Каждая коробка имеет номер (индекс). Нумерация начинается с 0 — первая коробка имеет номер 0, вторая — 1, и так далее.",
    examples: [
      {
        level: "minimal",
        code: "const fruits = ['яблоко', 'банан', 'вишня'];\n\nconsole.log(fruits[0]);    // 'яблоко'\nconsole.log(fruits.length); // 3",
        explanation: "Массив — список. Индекс начинается с 0. length — количество элементов."
      },
      {
        level: "simple",
        code: "const numbers = [10, 20, 30];\nnumbers.push(40);    // [10, 20, 30, 40]\nnumbers.pop();       // [10, 20, 30]\nnumbers.unshift(5);  // [5, 10, 20, 30]\nconsole.log(numbers); // [5, 10, 20, 30]",
        explanation: "push/pop — конец массива. unshift/shift — начало. Это основные методы мутации."
      },
      {
        level: "real",
        code: "const original = [1, 2, 3];\nconst copy = [...original];\n\ncopy.push(4);\nconsole.log(original); // [1, 2, 3] (не изменился!)\nconsole.log(copy);     // [1, 2, 3, 4]",
        explanation: "Spread (...) создаёт копию массива. Изменения copy не влияют на original."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что индекс начинается с 1",
        why: "В JavaScript индексы начинаются с 0. Первый элемент — arr[0], а не arr[1].",
        right: "arr[0] — первый элемент. arr[length-1] — последний."
      },
      {
        wrong: "Путать push/pop и unshift/shift",
        why: "push/pop работают с концом, unshift/shift — с началом.",
        right: "push → конец (добавить), pop ← конец (удалить). unshift → начало (добавить), shift ← начало (удалить)."
      },
      {
        wrong: "Думать, что length обновляется автоматически при удалении",
        why: "splice() меняет length, delete — нет (создаёт пустой слот).",
        right: "Используйте splice() для удаления, не delete."
      }
    ],
    importantToRemember: [
      "Индексы начинаются с 0",
      "length — количество элементов",
      "push/pop — конец, unshift/shift — начало",
      "Массивы передаются по ссылке",
      "Spread [...] создаёт копию"
    ],
    sources: [
      { title: "MDN: Array", url: "https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array" }
    ],
    connection: {
      back: "Вы знаете объекты (J11) — массивы это особый вид объекта для упорядоченных данных.",
      forward: "Следующий урок (J14) — базовые методы массивов: map, filter, find, reduce."
    }
  },

  // ============================================
  // J14 — Basic Array Methods
  // ============================================
  {
    slug: "basic-array-methods",
    track: "js-core",
    order: 15,
    title: "Основные методы массивов",
    summary: "Научиться использовать map, filter, find, reduce — четыре ключевых метода для работы с массивами, понимая, что каждый из них возвращает.",
    level: "Beginner",
    prerequisites: ["arrays"],
    learningObjective: "После этого урока вы сможете использовать map, filter, find и reduce и объяснять, что возвращает каждый метод.",
    shortExplanation: "map создаёт новый массив, преобразуя каждый элемент. filter создаёт новый массив с элементами, прошедшими проверку. find возвращает первый подходящий элемент. reduce сворачивает массив в одно значение.",
    detailedExplanation: "Сначала короткое, но важное объяснение: все четыре метода принимают функцию — её называют callback (функция-аргумент). Метод сам вызывает эту функцию для каждого элемента массива. Стрелочную запись x => ... вы уже знаете из J10 — здесь она используется именно как короткий способ передать такую функцию. Почему функции можно передавать в другие функции и как это устроено, подробно разберём в J16 — пока воспринимайте это как данность.\n\nЧетыре основных метода массивов:\n\n1. map(callback) — создаёт НОВЫЙ массив:\nconst numbers = [1, 2, 3];\nconst doubled = numbers.map(x => x * 2);\n// doubled = [2, 4, 6]\n// numbers не изменился!\n\n2. filter(callback) — создаёт НОВЫЙ массив с подходящими элементами:\nconst numbers = [1, 2, 3, 4, 5];\nconst even = numbers.filter(x => x % 2 === 0);\n// even = [2, 4]\n\n3. find(callback) — возвращает ПЕРВЫЙ подходящий элемент:\nconst users = [{name: 'Анна', age: 25}, {name: 'Пётр', age: 30}];\nconst petr = users.find(u => u.name === 'Пётр');\n// petr = {name: 'Пётр', age: 30}\n\n4. reduce(callback, initialValue) — сворачивает массив в одно значение:\nconst numbers = [1, 2, 3, 4];\nconst sum = numbers.reduce((acc, num) => acc + num, 0);\n// sum = 10\n\nПравило:\n- map → новый массив той же длины\n- filter → новый массив (короче или той же длины)\n- find → один элемент или undefined\n- reduce → одно значение (число, строка, объект, массив)",
    mentalModel: "map — как пропустить каждый элемент через трансформатор (конвейер). filter — как сито: пропускает только подходящее. find — как поиск: находит первое подходящее. reduce — как складывание: берёт все элементы и сворачивает в один результат.",
    examples: [
      {
        level: "minimal",
        code: "const numbers = [1, 2, 3];\nconst doubled = numbers.map(x => x * 2);\nconsole.log(doubled); // [2, 4, 6]",
        explanation: "map проходит по каждому элементу и создаёт новый массив с результатом."
      },
      {
        level: "simple",
        code: "const numbers = [1, 2, 3, 4, 5];\nconst even = numbers.filter(x => x % 2 === 0);\nconst firstBig = numbers.find(x => x > 3);\nconsole.log(even);    // [2, 4]\nconsole.log(firstBig); // 4",
        explanation: "filter возвращает массив подходящих. find возвращает первый подходящий."
      },
      {
        level: "real",
        code: "const orders = [\n  { product: 'Phone', price: 500 },\n  { product: 'Laptop', price: 1200 },\n  { product: 'Cable', price: 15 }\n];\n\n// Общая стоимость дорогих заказов (>100):\nconst total = orders\n  .filter(o => o.price > 100)\n  .reduce((sum, o) => sum + o.price, 0);\n\nconsole.log(total); // 1700",
        explanation: "Цепочка методов: сначала фильтруем, потом суммируем. Каждый метод возвращает результат для следующего."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что map изменяет исходный массив",
        why: "map создаёт новый массив. Исходный не меняется.",
        right: "map возвращает НОВЫЙ массив. Присвойте результат переменной."
      },
      {
        wrong: "Путать find и filter",
        why: "find возвращает один элемент, filter — массив.",
        right: "find → один элемент. filter → массив."
      },
      {
        wrong: "Забывать initialValue в reduce",
        why: "Без initialValue и пустом массиве — TypeError. Всегда указывайте начальное значение.",
        right: "reduce((acc, val) => ..., 0) — всегда указывайте initialValue."
      }
    ],
    importantToRemember: [
      "map → новый массив (трансформация)",
      "filter → новый массив (фильтрация)",
      "find → один элемент или undefined",
      "reduce → одно значение (сворачивание)",
      "Все методы не изменяют исходный массив"
    ],
    connection: {
      back: "Вы знаете массивы (J13) — теперь вы умеете эффективно с ними работать.",
      forward: "Следующий урок (J15) — перебор массивов: forEach и for...of подробнее."
    }
  },

  // ============================================
  // J15 — Array Iteration
  // ============================================
  {
    slug: "array-iteration",
    track: "js-core",
    order: 16,
    title: "Перебор массивов",
    summary: "Научиться перебирать массивы с помощью forEach, for...of, findIndex, some, every и includes — понимая, когда использовать каждый.",
    level: "Beginner",
    prerequisites: ["basic-array-methods"],
    learningObjective: "После этого урока вы сможете перебирать массивы через forEach и for...of, а также использовать findIndex, some, every, includes для проверки условий.",
    shortExplanation: "forEach — перебирает каждый элемент (без return). for...of — перебирает элементы с возможностью break. some — проверяет, есть ли хотя бы один подходящий. every — проверяет, все ли подходят. includes — проверяет наличие элемента.",
    detailedExplanation: "forEach(callback) — выполняет действие для каждого элемента:\nconst fruits = ['яблоко', 'банан'];\nfruits.forEach(fruit => console.log(fruit));\n// Выведет каждое название. Возвращает undefined!\n\nfor...of — перебор с возможностью break:\nfor (const fruit of fruits) {\n  if (fruit === 'банан') break;\n  console.log(fruit);\n}\n\nsome(callback) — возвращает true, если хотя бы один элемент проходит проверку:\n[1, 2, 3].some(x => x > 2); // true (3 > 2)\n\nevery(callback) — возвращает true, если ВСЕ элементы проходят проверку:\n[1, 2, 3].every(x => x > 0); // true\n[1, 2, 3].every(x => x > 1); // false (1 не > 1)\n\nincludes(value) — проверяет наличие элемента:\n['а', 'б', 'в'].includes('б'); // true\n\nfindIndex(callback) — возвращает индекс первого подходящего:\n[10, 20, 30].findIndex(x => x === 20); // 1",
    mentalModel: "forEach — как проверить каждый предмет на полке (просто посмотреть). some/every — как задать вопрос: «хотя бы один?» / «все ли?». includes — как спросить: «есть ли это здесь?».",
    examples: [
      {
        level: "minimal",
        code: "const numbers = [1, 2, 3];\nnumbers.forEach(n => console.log(n)); // 1, 2, 3",
        explanation: "forEach перебирает каждый элемент и выполняет функцию. Не возвращает ничего."
      },
      {
        level: "simple",
        code: "const ages = [15, 22, 17, 30];\n\nconst hasAdult = ages.some(age => age >= 18);\nconst allAdult = ages.every(age => age >= 18);\n\nconsole.log(hasAdult); // true (22 и 30)\nconsole.log(allAdult); // false (15 и 17)",
        explanation: "some — «хотя бы один?». every — «все ли?»."
      },
      {
        level: "real",
        code: "const users = [\n  { name: 'Анна', active: true },\n  { name: 'Пётр', active: false },\n  { name: 'Мария', active: true }\n];\n\nconst activeCount = users.filter(u => u.active).length;\nconst hasInactive = users.some(u => !u.active);\n\nconsole.log(activeCount); // 2\nconsole.log(hasInactive);  // true",
        explanation: "Цепочки методов позволяют выразить сложную логику в одной строке."
      }
    ],
    commonMistakes: [
      {
        wrong: "Использовать break/continue в forEach",
        why: "forEach — это не цикл. break/continue не работают.",
        right: "Для break/continue используйте for...of."
      },
      {
        wrong: "Думать, что forEach возвращает массив",
        why: "forEach всегда возвращает undefined.",
        right: "Для создания нового массива используйте map."
      },
      {
        wrong: "Забывать, что some/every возвращают boolean",
        why: "some/every — это проверки, они не модифицируют массив.",
        right: "some/every → true/false. Для фильтрации — filter."
      }
    ],
    importantToRemember: [
      "forEach — перебор без return",
      "for...of — перебор с break/continue",
      "some — хотя бы один true → true",
      "every — все true → true",
      "includes — проверка наличия элемента"
    ],
    connection: {
      back: "Вы знаете map/filter/find (J14) — теперь вы знаете полный набор методов перебора.",
      forward: "Следующий урок (J16) — функции высшего порядка: передача функций как аргументов."
    }
  },

  // ============================================
  // J16 — Higher-Order Functions
  // ============================================
  {
    slug: "higher-order-functions",
    track: "js-core",
    order: 17,
    title: "Функции высшего порядка",
    summary: "Понять, что функции в JavaScript — это значения, и научиться передавать функции как аргументы и возвращать их из других функций.",
    level: "Beginner",
    prerequisites: ["array-iteration"],
    learningObjective: "После этого урока вы сможете передавать функции как аргументы, возвращать функции из других функций и объяснять, почему функции высшего порядка так полезны.",
    shortExplanation: "Функция высшего порядка — это функция, которая принимает другую функцию как аргумент или возвращает функцию. map, filter, find — все они принимают callback-функции. Это основа functional programming в JavaScript.",
    detailedExplanation: "В JavaScript функции — это значения. Их можно:\n- Передавать как аргументы\n- Возвращать из других функций\n- Сохранять в переменные\n\nФункция высшего порядка (higher-order function):\n- Принимает функцию как аргумент\n- ИЛИ возвращает функцию\n\nПримеры встроеных higher-order functions:\n- [1,2,3].map(x => x * 2) — map принимает функцию\n- [1,2,3].filter(x => x > 1) — filter принимает функцию\n- setTimeout(() => {...}, 1000) — setTimeout принимает функцию\n\nСоздание собственной higher-order функции:\nfunction repeat(n, action) {\n  for (let i = 0; i < n; i++) {\n    action(i);\n  }\n}\nrepeat(3, console.log); // 0, 1, 2\n\nВозврат функции:\nfunction multiplier(factor) {\n  return (x) => x * factor;\n}\nconst double = multiplier(2);\nconst triple = multiplier(3);\ndouble(5); // 10\ntriple(5); // 15\n\nЗачем это нужно:\n- Повторное использование логики\n- Абстракция (скрыть детали)\n- Декларативный стиль (описать ЧТО, а не КАК)",
    mentalModel: "Higher-order функция — как фабрика, которая делает фабрики. Вы даёте ей «рецепт» (функцию), а она создаёт новый инструмент, который использует этот рецепт.",
    examples: [
      {
        level: "minimal",
        code: "function doTwice(fn) {\n  fn();\n  fn();\n}\n\ndoTwice(() => console.log('Привет!'));\n// Привет!\n// Привет!",
        explanation: "doTwice — higher-order функция. Она принимает функцию и вызывает её дважды."
      },
      {
        level: "simple",
        code: "function createGreeter(greeting) {\n  return (name) => `${greeting}, ${name}!`;\n}\n\nconst greetHello = createGreeter('Привет');\nconst greetBye = createGreeter('Пока');\n\nconsole.log(greetHello('Анна')); // Привет, Анна!\nconsole.log(greetBye('Пётр'));   // Пока, Пётр!",
        explanation: "createGreeter возвращает новую функцию. Каждая «кастомная» функция использует свой greeting."
      },
      {
        level: "real",
        code: "function createValidator(validateFn) {\n  return (value) => {\n    const isValid = validateFn(value);\n    return { value, isValid, errors: isValid ? [] : ['Invalid'] };\n  };\n}\n\nconst isEmail = createValidator(v => v.includes('@'));\nconst isPositive = createValidator(v => v > 0);\n\nconsole.log(isEmail('test@test.com')); // { isValid: true, ... }\nconsole.log(isPositive(-5));           // { isValid: false, ... }",
        explanation: "Higher-order функции позволяют создавать reusable валидаторы, логгеры, обработчики —anything."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что callback — это особый тип",
        why: "Callback — это просто функция, переданная как аргумент. Ничего особенного.",
        right: "Callback — это любая функция, переданная другой функции."
      },
      {
        wrong: "Не понимать, зачем возвращать функцию",
        why: "Возврат функции позволяет создавать «кастомные» функции с предустановленными настройками.",
        right: "Возврат функции — это factory pattern: создаёте функцию с предустановленным поведением."
      },
      {
        wrong: "Путать map с forEach",
        why: "map возвращает новый массив, forEach — undefined.",
        right: "map → новый массив. forEach → undefined (просто побочный эффект)."
      }
    ],
    importantToRemember: [
      "Функции — это значения, их можно передавать и возвращать",
      "Higher-order: принимает функцию ИЛИ возвращает функцию",
      "map, filter, find — примеры higher-order functions",
      "Callback — просто функция как аргумент",
      "Возврат функции позволяет создавать «кастомные» инструменты"
    ],
    connection: {
      back: "Вы знаете методы массивов (J14, J15) — теперь вы понимаете, почему они работают именно так.",
      forward: "Следующий урок (J17) — Object.keys/values/entries: как перебирать свойства объекта."
    }
  },

  // ============================================
  // J17 — Object.keys/values/entries
  // ============================================
  {
    slug: "object-keys-values-entries",
    track: "js-core",
    order: 18,
    title: "Object.keys, values, entries",
    summary: "Научиться перебирать свойства объектов с помощью Object.keys(), Object.values() и Object.entries().",
    level: "Beginner",
    prerequisites: ["higher-order-functions"],
    learningObjective: "После этого урока вы сможете получать ключи, значения или пары из объекта и перебирать их.",
    shortExplanation: "Object.keys() возвращает массив ключей. Object.values() — массив значений. Object.entries() — массив пар [ключ, значение]. Эти методы позволяют перебирать объекты так же удобно, как массивы.",
    detailedExplanation: "Объекты в JavaScript не имеют «индексов» как массивы. Но Object.keys/values/entries превращают их в массивы, которые можно перебирать.\n\nObject.keys(obj) — массив ключей:\nconst user = { name: 'Анна', age: 25 };\nObject.keys(user); // ['name', 'age']\n\nObject.values(obj) — массив значений:\nObject.values(user); // ['Анна', 25]\n\nObject.entries(obj) — массив пар [ключ, значение]:\nObject.entries(user); // [['name', 'Анна'], ['age', 25]]\n\nЭти методы возвращают массивы, поэтому с ними работают все методы массивов:\nObject.keys(user).forEach(key => console.log(key));\nObject.entries(user).filter(([k, v]) => typeof v === 'string');\n\nПрименение:\n- Перебор свойств объекта\n- Фильтрация свойств\n- Преобразование объекта в массив\n- Проверка наличия свойства",
    mentalModel: "Object.keys/values/entries — как ключ к шкафу: keys показывают названия ящиков, values — содержимое, entries — пары «название → содержимое».",
    examples: [
      {
        level: "minimal",
        code: "const user = { name: 'Анна', age: 25, city: 'Москва' };\n\nconsole.log(Object.keys(user));   // ['name', 'age', 'city']\nconsole.log(Object.values(user)); // ['Анна', 25, 'Москва']",
        explanation: "keys — ключи, values — значения. Оба возвращают массивы."
      },
      {
        level: "simple",
        code: "const product = { name: 'Phone', price: 500, inStock: true };\n\nconst entries = Object.entries(product);\n// [['name', 'Phone'], ['price', 500], ['inStock', true]]\n\nentries.forEach(([key, value]) => {\n  console.log(`${key}: ${value}`);\n});",
        explanation: "Object.entries + деструктуризация — удобный способ перебрать пары ключ/значение."
      },
      {
        level: "real",
        code: "const scores = { Анна: 95, Пётр: 87, Мария: 92 };\n\n// Найти лучший результат:\nconst topScorer = Object.entries(scores)\n  .sort(([, a], [, b]) => b - a)[0];\n\nconsole.log(`Лучший: ${topScorer[0]} (${topScorer[1]})`);\n// Лучший: Анна (95)",
        explanation: "Object.entries превращает объект в массив, который можно сортировать и обрабатывать."
      }
    ],
    commonMistakes: [
      {
        wrong: "Путать Object.keys и Object.values",
        why: "keys — ключи (строки), values — значения (любой тип).",
        right: "Object.keys → ['name', 'age']. Object.values → ['Анна', 25]."
      },
      {
        wrong: "Думать, что Object.entries возвращает объект",
        why: "Object.entries возвращает массив массивов: [['key', 'value']].",
        right: "Object.entries → [['key', 'value']]. Для обратного преобразования — Object.fromEntries."
      },
      {
        wrong: "Забывать, что порядок свойств может отличаться",
        why: "В объектах порядок свойств не гарантирован (хотя на практике обычно строковые ключи идут в порядке объявления).",
        right: "Не полагайтесь на порядок свойств объекта. Используйте массивы для упорядоченных данных."
      }
    ],
    importantToRemember: [
      "Object.keys → массив ключей",
      "Object.values → массив значений",
      "Object.entries → массив [ключ, значение]",
      "Возвращают массивы — работают с map/filter/forEach",
      "Object.fromEntries — обратное преобразование"
    ],
    connection: {
      back: "Вы знаете объекты (J11) и методы массивов (J14-16) — теперь вы можете их комбинировать.",
      forward: "Следующий урок (J18) — деструктуризация и spread/rest: мощный синтаксис для массивов и объектов."
    }
  },

  // ============================================
  // J18 — Destructuring + Spread/Rest
  // ============================================
  {
    slug: "destructuring-spread-rest",
    track: "js-core",
    order: 19,
    title: "Деструктуризация и spread/rest",
    summary: "Научиться извлекать значения из массивов и объектов через деструктуризацию, а также использовать spread для копирования и rest для сбора остатка.",
    level: "Beginner",
    prerequisites: ["object-keys-values-entries"],
    learningObjective: "После этого урока вы сможете деструктурировать массивы и объекты, использовать spread для копирования и объединения и rest для сбора оставшихся элементов.",
    shortExplanation: "Деструктуризация — способ разложить массив или объект на отдельные переменные: const { name, age } = user. Spread (...) — разворачивает массив/объект: [...arr1, ...arr2]. Rest (...) — собирает остаток: const [first, ...rest] = arr.",
    detailedExplanation: "Деструктуризация массивов:\nconst [a, b, c] = [1, 2, 3];\n// a = 1, b = 2, c = 3\n\nПропуск элементов:\nconst [first, , third] = [1, 2, 3];\n// first = 1, third = 3\n\nДеструктуризация объектов:\nconst { name, age } = { name: 'Анна', age: 25 };\n// name = 'Анна', age = 25\n\nС переименованием:\nconst { name: userName } = { name: 'Анна' };\n// userName = 'Анна'\n\nС значением по умолчанию:\nconst { name, age = 0 } = { name: 'Анна' };\n// name = 'Анна', age = 0\n\nSpread (...) — разворачивает:\nconst arr1 = [1, 2];\nconst arr2 = [3, 4];\nconst merged = [...arr1, ...arr2]; // [1, 2, 3, 4]\n\nconst obj1 = { a: 1 };\nconst obj2 = { b: 2 };\nconst mergedObj = { ...obj1, ...obj2 }; // { a: 1, b: 2 }\n\nRest (...) — собирает остаток:\nconst [first, ...rest] = [1, 2, 3, 4];\n// first = 1, rest = [2, 3, 4]\n\nconst { name, ...rest } = { name: 'Анна', age: 25, city: 'Москва' };\n// name = 'Анна', rest = { age: 25, city: 'Москва' }",
    mentalModel: "Деструктуризация — как распаковка подарка: открываете коробку (массив/объект) и достаёте конкретные вещи (переменные). Spread — как высыпать содержимое коробки. Rest — как собрать всё, что не достали по отдельности.",
    examples: [
      {
        level: "minimal",
        code: "const [a, b] = [10, 20];\nconsole.log(a); // 10\nconsole.log(b); // 20",
        explanation: "Деструктуризация массива: разложили на две переменные."
      },
      {
        level: "simple",
        code: "const user = { name: 'Анна', age: 25, city: 'Москва' };\nconst { name, age } = user;\nconsole.log(name); // 'Анна'\nconsole.log(age);  // 25",
        explanation: "Деструктуризация объекта: извлекаем name и age, city игнорируется."
      },
      {
        level: "real",
        code: "function createUser(name, options = {}) {\n  const { age = 0, active = true } = options;\n  return { name, age, active };\n}\n\nconst user = createUser('Анна', { age: 25 });\nconsole.log(user); // { name: 'Анна', age: 25, active: true }",
        explanation: "Деструктуризация + значения по умолчанию — мощный паттерн для параметров функций."
      }
    ],
    commonMistakes: [
      {
        wrong: "Путать spread и rest",
        why: "Spread разворачивает (...arr), rest собирает (...args). Символ один — смысл противоположный.",
        right: "Spread: const arr2 = [...arr1]. Rest: const [first, ...rest] = arr."
      },
      {
        wrong: "Забывать, что rest должен быть последним",
        why: "const [a, ...rest, b] = [1,2,3,4]; — SyntaxError! Rest не может быть в середине.",
        right: "Rest должен быть последним элементом: const [a, ...rest] = arr."
      },
      {
        wrong: "Думать, что spread создаёт глубокую копию",
        why: "Spread создаёт поверхностную копию. Вложенные объекты остаются ссылками.",
        right: "Для глубокой копии: structuredClone(obj) или JSON.parse(JSON.stringify(obj))."
      }
    ],
    importantToRemember: [
      "Деструктуризация извлекает значения в переменные",
      "Spread (...) разворачивает массив/объект",
      "Rest (...) собирает остаток",
      "Spread создаёт поверхностную копию",
      "Rest должен быть последним элементом"
    ],
    connection: {
      back: "Вы знаете объекты и массивы (J11-J17) — теперь у вас есть мощный синтаксис для работы с ними.",
      forward: "Следующий урок (J19) — Date: работа с датами и временем в JavaScript."
    }
  },

  // ============================================
  // J19 — Date
  // ============================================
  {
    slug: "date",
    track: "js-core",
    order: 20,
    title: "Дата и время (Date)",
    summary: "Научиться работать с датами и временем в JavaScript: создавать, форматировать, вычислять разницу между датами.",
    level: "Beginner",
    prerequisites: ["destructuring-spread-rest"],
    learningObjective: "После этого урока вы сможете создавать объекты Date, извлекать компоненты даты, форматировать даты и вычислять разницу между датами.",
    shortExplanation: "Date — встроенный объект для работы с датами и временем. new Date() создаёт объект с текущей датой. Методы позволяют получить год, месяц, день, часы, минуты и т.д.",
    detailedExplanation: "Создание Date:\nnew Date() — текущая дата и время\nnew Date('2025-01-15') — конкретная дата\nnew Date(2025, 0, 15) — год, месяц (с 0!), день\nnew Date(2025, 0, 15, 10, 30) — + часы, минуты\n\nВажно: месяцы нумеруются с 0!\nJanuary = 0, February = 1, ..., December = 11\n\nОсновные методы:\n.getFullYear() — год\n.getMonth() — месяц (с 0!)\n.getDate() — день месяца\n.getHours() — часы\n.getMinutes() — минуты\n.getSeconds() — секунды\n.getDay() — день недели (0 = воскресенье)\n\nФорматирование:\n.toLocaleDateString() — '15.01.2025'\n.toLocaleTimeString() — '10:30:00'\n.toISOString() — '2025-01-15T10:30:00.000Z'\n\nВычисления:\nconst diff = date2 - date1; // разница в миллисекундах\nMath.floor(diff / (1000 * 60 * 60 * 24)); // дни",
    mentalModel: "Date — как календарь с часами. Вы создаёте дату (открываете календарь на определённый день) и спрашиваете: «Какой сегодня месяц? Сколько времени?»",
    examples: [
      {
        level: "minimal",
        code: "const now = new Date();\nconsole.log(now); // Mon Jan 15 2025 10:30:00\nconsole.log(now.getFullYear()); // 2025\nconsole.log(now.getMonth());    // 0 (January!)\nconsole.log(now.getDate());     // 15",
        explanation: "new Date() создаёт объект с текущей датой. Месяцы начинаются с 0."
      },
      {
        level: "simple",
        code: "const birthday = new Date(2000, 5, 15); // 15 июня 2000\n\nconst today = new Date();\nconst age = today.getFullYear() - birthday.getFullYear();\n\nconsole.log(`Возраст: ${age} лет`);",
        explanation: "Вычисляем возраст, вычитая год рождения из текущего года."
      },
      {
        level: "real",
        code: "function formatRelative(date) {\n  const now = new Date();\n  const diff = now - date;\n  const days = Math.floor(diff / (1000 * 60 * 60 * 24));\n\n  if (days === 0) return 'Сегодня';\n  if (days === 1) return 'Вчера';\n  if (days < 7) return `${days} дней назад`;\n  return date.toLocaleDateString('ru-RU');\n}\n\nconsole.log(formatRelative(new Date())); // 'Сегодня'",
        explanation: "Функция форматирования даты: определяет, как давно была создана дата."
      }
    ],
    commonMistakes: [
      {
        wrong: "Забывать, что месяцы начинаются с 0",
        why: "getMonth() для января возвращает 0, а не 1. Это частый источник ошибок.",
        right: "January = 0, February = 1, ..., December = 11."
      },
      {
        wrong: "Сравнивать даты через ===",
        why: "Date — это объект. Два new Date() с одинаковой датой — разные объекты (=== не сработает).",
        right: "Сравнивайте через getTime(): date1.getTime() === date2.getTime()."
      },
      {
        wrong: "Использовать Date.parse() для валидации",
        why: "Date.parse() может вернуть NaN для невалидных дат, но это неочевидно.",
        right: "Проверяйте: !isNaN(date.getTime())."
      }
    ],
    importantToRemember: [
      "Месяцы нумеруются с 0 (January = 0!)",
      "Date — объект, сравнивайте через getTime()",
      "Разница дат — в миллисекундах",
      "toLocaleDateString() для красивого форматирования"
    ],
    connection: {
      back: "Вы знаете объекты и методы (J11-J18) — Date это ещё один встроенный объект.",
      forward: "Следующий урок (J20) — JSON: как превращать данные в текст и обратно."
    }
  },

  // ============================================
  // J20 — JSON
  // ============================================
  {
    slug: "json",
    track: "js-core",
    order: 21,
    title: "JSON",
    summary: "Научиться преобразовывать данные в текст (JSON.stringify) и обратно (JSON.parse), понимая, зачем нужен JSON и его ограничения.",
    level: "Beginner",
    prerequisites: ["date"],
    learningObjective: "После этого урока вы сможете преобразовывать значения JavaScript в JSON-строки и обратно и объяснять роль JSON в обмене данными.",
    shortExplanation: "JSON (JavaScript Object Notation) — текстовый формат для обмена данными. JSON.stringify() превращает данные в текст. JSON.parse() превращает текст обратно в данные. JSON используется повсеместно: API, файлы конфигурации, localStorage.",
    detailedExplanation: "JSON — это текстовый формат:\n- Объекты: { \"key\": \"value\" } (ключи в кавычках!)\n- Массивы: [1, 2, 3]\n- Строки: \"text\" (двойные кавычки)\n- Числа: 42\n- Boolean: true/false\n- null\n\nJSON.stringify(value) — данные → текст:\nconst user = { name: 'Анна', age: 25 };\nconst json = JSON.stringify(user);\n// '{\"name\":\"Анна\",\"age\":25}'\n\nJSON.parse(text) — текст → данные:\nconst data = JSON.parse(json);\n// { name: 'Анна', age: 25 }\n\nОграничения JSON:\n- Только строки, числа, boolean, null, массивы, объекты\n- Ключи ТОЛЬКО в двойных кавычках\n- Нет: undefined, функции, Date, Symbol\n\nJSON + localStorage:\nlocalStorage.setItem('user', JSON.stringify(user));\nconst saved = JSON.parse(localStorage.getItem('user'));",
    mentalModel: "JSON — как переводчик между JavaScript и другими языками. Он превращает JavaScript-данные в текст, который понимает любой язык программирования.",
    examples: [
      {
        level: "minimal",
        code: "const data = { name: 'Анна', age: 25 };\nconst json = JSON.stringify(data);\nconsole.log(json); // '{\"name\":\"Анна\",\"age\":25}'\n\nconst back = JSON.parse(json);\nconsole.log(back.name); // 'Анна'",
        explanation: "JSON.stringify превращает объект в текст. JSON.parse — обратно."
      },
      {
        level: "simple",
        code: "const users = [\n  { name: 'Анна', age: 25 },\n  { name: 'Пётр', age: 30 }\n];\n\nconst json = JSON.stringify(users, null, 2);\nconsole.log(json);\n// [\n//   { \"name\": \"Анна\", \"age\": 25 },\n//   { \"name\": \"Пётр\", \"age\": 30 }\n// ]",
        explanation: "Третий аргумент — количество пробелов для форматирования (pretty print)."
      },
      {
        level: "real",
        code: "const user = {\n  name: 'Анна',\n  greet: function() { return 'Привет'; }\n};\n\nconst json = JSON.stringify(user);\nconst back = JSON.parse(json);\n\nconsole.log(back); // { name: 'Анна' }\nconsole.log(back.greet); // undefined (функции не сериализуются!)",
        explanation: "JSON.stringify теряет функции, undefined, Symbol. Это ограничение формата."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что JSON — это JavaScript",
        why: "JSON — это текстовый формат данных. Он похож на JavaScript, но не является им.",
        right: "JSON — формат данных. JavaScript — язык программирования."
      },
      {
        wrong: "Использовать одинарные кавычки в JSON",
        why: "JSON требует двойные кавычки для ключей и строк: { \"key\": \"value\" }",
        right: "JSON: двойные кавычки. JavaScript: любые кавычки."
      },
      {
        wrong: "Забывать, что JSON.stringify() теряет функции",
        why: "Функции, undefined, Symbol, Date (без кастомного сериализатора) не попадают в JSON.",
        right: "Проверяйте, что данные совместимы с JSON перед сериализацией."
      }
    ],
    importantToRemember: [
      "JSON.stringify() — данные → текст",
      "JSON.parse() — текст → данные",
      "JSON: строки, числа, boolean, null, массивы, объекты",
      "Ключи ТОЛЬКО в двойных кавычках",
      "Функции и undefined не сериализуются"
    ],
    connection: {
      back: "Вы знаете объекты и массивы (J11-J18) — JSON это формат, которым мы обмениваемся такими данными.",
      forward: "Следующий урок (J21) — методы чисел: инструменты для работы с числами."
    }
  },

  // ============================================
  // J21 — Number Methods
  // ============================================
  {
    slug: "number-methods",
    track: "js-core",
    order: 22,
    title: "Методы чисел",
    summary: "Научиться работать с числами: форматирование, округление, преобразование строк в числа и проверка на числа.",
    level: "Beginner",
    prerequisites: ["json"],
    learningObjective: "После этого урока вы сможете использовать методы чисел, парсить целые числа, округлять числа и корректно обрабатывать NaN.",
    shortExplanation: "Number — встроенный объект для работы с числами. parseInt() и parseFloat() преобразуют строки в числа. toFixed() форматирует десятичные. isNaN() проверяет на NaN. Math содержит математические функции: floor, ceil, round, random.",
    detailedExplanation: "Преобразование строк в числа:\nNumber('42')     // 42\nNumber('hello')  // NaN\nparseInt('42px') // 42 (отбрасывает нечисловые символы)\nparseFloat('3.14abc') // 3.14\n\nОкругление:\nMath.floor(3.7)  // 3 (вниз)\nMath.ceil(3.2)   // 4 (вверх)\nMath.round(3.5)  // 4 (ближайшее)\n\nФорматирование:\n(3.14159).toFixed(2)  // '3.14' (строка!)\n(3.14159).toPrecision(3) // '3.14'\n\nПроверка:\nNumber.isNaN(NaN)    // true\nNumber.isNaN('hello') // false\nNumber.isFinite(42)   // true\nNumber.isFinite(Infinity) // false\n\nMath:\nMath.abs(-5)    // 5\nMath.max(1, 5, 3) // 5\nMath.min(1, 5, 3) // 1\nMath.random()   // 0 ≤ x < 1\nMath.sqrt(16)   // 4\nMath.pow(2, 3)  // 8",
    mentalModel: "Number methods — как калькулятор с дополнительными кнопками: округление, форматирование, проверка — всё для работы с числами.",
    examples: [
      {
        level: "minimal",
        code: "console.log(parseInt('42px'));    // 42\nconsole.log(parseFloat('3.14abc')); // 3.14\nconsole.log(Number('hello'));       // NaN",
        explanation: "parseInt и parseFloat «вытаскивают» число из строки. Если не могут — NaN."
      },
      {
        level: "simple",
        code: "const price = 19.999;\nconsole.log(price.toFixed(2)); // '20.00'\nconsole.log(Math.round(price)); // 20\nconsole.log(Math.floor(price)); // 19",
        explanation: "toFixed возвращает строку! Math.round/floor/ceil возвращают число."
      },
      {
        level: "real",
        code: "function generateRandomId() {\n  return Math.random().toString(36).substring(2, 9);\n}\n\nconsole.log(generateRandomId()); // 'f4ki2j1'",
        explanation: "Math.random() генерирует случайное число. toString(36) превращает его в строку."
      }
    ],
    commonMistakes: [
      {
        wrong: "Забывать, что toFixed() возвращает строку",
        why: "(3.14).toFixed(2) → '3.14' (строка, не число!).",
        right: "Используйте Number((3.14).toFixed(2)) для числа."
      },
      {
        wrong: "Использовать isNaN() вместо Number.isNaN()",
        why: "isNaN() приводит значение к числу перед проверкой. Number.isNaN() — строгая проверка.",
        right: "Используйте Number.isNaN() для точной проверки."
      },
      {
        wrong: "Думать, что parseInt всегда точен",
        why: "parseInt('08') в старых IE возвращает 0 (восьмеричная система). Всегда указывайте основание: parseInt('08', 10).",
        right: "Всегда: parseInt(value, 10)."
      }
    ],
    importantToRemember: [
      "parseInt/parseFloat — строки → числа",
      "toFixed() возвращает строку!",
      "Math.floor/ceil/round — округление",
      "Number.isNaN() — строгая проверка на NaN",
      "Math.random() — случайное число 0 ≤ x < 1"
    ],
    connection: {
      back: "Вы знаете базовые операции (J4) — теперь у вас есть продвинутые инструменты для чисел.",
      forward: "Следующий урок (J22) — методы строк: инструменты для работы с текстом."
    }
  },

  // ============================================
  // J22 — String Methods
  // ============================================
  {
    slug: "string-methods",
    track: "js-core",
    order: 23,
    title: "Методы строк",
    summary: "Научиться работать со строками: поиск, замена, разрезание, объединение, форматирование — понимая, что строки неизменяемы.",
    level: "Beginner",
    prerequisites: ["number-methods"],
    learningObjective: "После этого урока вы сможете использовать методы строк для поиска, изменения и форматирования текста и объяснять, почему строки неизменяемы (immutable).",
    shortExplanation: "Строки в JavaScript неизменяемы (immutable). Методы возвращают новые строки, не меняя оригинал. Основные: length, includes, indexOf, slice, replace, split, join, toUpperCase, trim.",
    detailedExplanation: "Строки immutable:\nconst str = 'hello';\nstr.toUpperCase(); // 'HELLO' (оригинал не меняется!)\n\nОсновные методы:\n.length — длина\n.includes('lo') — содержит ли подстроку\n.indexOf('lo') — индекс подстроки (-1 если нет)\n.startsWith('he') — начинается ли с\n.endsWith('lo') — заканчивается ли на\n\nИзвлечение:\n.slice(1, 3) — символы с индекса 1 по 2 (третий не включается)\n.slice(2) — с индекса 2 до конца\n.slice(-3) — последние 3 символа\n\nПреобразование:\n.toUpperCase() — в верхний регистр\n.toLowerCase() — в нижний регистр\n.trim() — убрать пробелы по краям\n.trimStart() / trimEnd() — только начало/конец\n\nПоиск и замена:\n.replace('old', 'new') — замена первого вхождения\n.replaceAll('old', 'new') — замена всех вхождений\n.split(',') — разбить строку в массив\nArray.join(', ') — объединить массив в строку\n\nШаблонные строки (backticks):\n`Привет, ${name}!` — интерполяция\n`Много\nстрок` — многострочная строка",
    mentalModel: "Строка — как каменная скрижаль. Вы не можете стереть букву — можно только создать новую скрижаль с изменениями. Методы строк — это「создатели новых скрижалей».",
    examples: [
      {
        level: "minimal",
        code: "const name = 'Анна';\nconsole.log(name.length);       // 4\nconsole.log(name.toUpperCase()); // 'АННА'\nconsole.log(name.includes('н')); // true",
        explanation: "Базовые свойства и методы строк."
      },
      {
        level: "simple",
        code: "const email = '  User@Example.COM  ';\nconst clean = email.trim().toLowerCase();\nconsole.log(clean); // 'user@example.com'",
        explanation: "Цепочка методов: trim убирает пробелы, toLowerCase приводит к нижнему регистру."
      },
      {
        level: "real",
        code: "function slugify(text) {\n  return text\n    .toLowerCase()\n    .trim()\n    .replace(/\\s+/g, '-')\n    .replace(/[^a-z0-9-]/g, '');\n}\n\nconsole.log(slugify('  Hello World!  ')); // 'hello-world'\nconsole.log(slugify('JavaScript 101'));   // 'javascript-101'",
        explanation: "Реальный пример: превращение заголовка в URL-friendly slug."
      }
    ],
    commonMistakes: [
      {
        wrong: "Думать, что строки изменяемы",
        why: "str[0] = 'H' — не работает! Строки immutable.",
        right: "Строки immutable. Создавайте новую: str.replace('h', 'H')."
      },
      {
        wrong: "Забывать, что slice не включает конечный индекс",
        why: "'hello'.slice(1, 3) → 'el' (индексы 1 и 2, третий не включается).",
        right: "slice(begin, end) — end не включается."
      },
      {
        wrong: "Путать split/join",
        why: "split превращает строку в массив. join — массив в строку.",
        right: "split → массив. join → строка."
      }
    ],
    importantToRemember: [
      "Строки immutable — методы возвращают новые",
      "includes/indexOf — поиск подстроки",
      "slice — извлечение части строки",
      "split/join — строка ↔ массив",
      "trim убирает пробелы по краям"
    ],
    connection: {
      back: "Вы знаете числа (J21) — теперь у вас есть похожие инструменты для текста.",
      forward: "Следующий урок (J23) — try/catch: корректная обработка ошибок."
    }
  },

  // ============================================
  // J23 — try/catch
  // ============================================
  {
    slug: "try-catch",
    track: "js-core",
    order: 24,
    title: "Обработка ошибок: try/catch",
    summary: "Научиться перехватывать ошибки с помощью try/catch/finally, понимая, когда это нужно и как работает механизм исключений.",
    level: "Beginner",
    prerequisites: ["string-methods"],
    learningObjective: "После этого урока вы сможете использовать try/catch для обработки ошибок, получать сообщения об ошибках и использовать finally для завершающих действий.",
    shortExplanation: "try/catch позволяет перехватывать ошибки, не останавливая всю программу. Код в try выполняется. Если происходит ошибка — выполняется catch. finally выполняется всегда, независимо от ошибки.",
    detailedExplanation: "Без try/catch:\nJSON.parse('невалидный JSON'); // Ошибка! Весь скрипт останавливается.\n\nС try/catch:\ntry {\n  JSON.parse('невалидный JSON');\n} catch (error) {\n  console.log('Ошибка:', error.message);\n}\n// Программа продолжает работу!\n\nСинтаксис:\ntry {\n  // код, который может вызвать ошибку\n} catch (error) {\n  // выполняется при ошибке\n  // error.message — описание ошибки\n  // error.name — тип ошибки\n} finally {\n  // выполняется ВСЕГДА\n}\n\nТипы ошибок:\n- Error — базовый тип\n- TypeError — неправильный тип\n- ReferenceError — необъявленная переменная\n- SyntaxError — синтаксическая ошибка\n- RangeError — значение вне диапазона\n\nThrow — создание собственных ошибок:\nthrow new Error('Что-то пошло не так');\nthrow new TypeError('Ожидается число');\n\nКогда использовать try/catch:\n- Работа с JSON.parse()\n- Доступ к неизвестным данным (API, пользовательский ввод)\n- Операции с файлами\n- Когда ошибка — ожидаемая ситуация",
    mentalModel: "try/catch — как подушка безопасности. Вы прыгаете с высоты (рискованный код), но если что-то пойдёт не так — подушка поймает вас (catch) и не даст разбиться.",
    examples: [
      {
        level: "minimal",
        code: "try {\n  console.log('Начало');\n  JSON.parse('не JSON');\n  console.log('Конец'); // Не выполнится!\n} catch (error) {\n  console.log('Ошибка:', error.message);\n}\n// Начало\n// Ошибка: Unexpected token... ",
        explanation: "catch перехватывает ошибку, и программа продолжает работу."
      },
      {
        level: "simple",
        code: "function parseData(jsonString) {\n  try {\n    return JSON.parse(jsonString);\n  } catch (error) {\n    console.error('Невалидный JSON:', error.message);\n    return null;\n  }\n}\n\nparseData('{\"name\": \"Анна\"}'); // { name: 'Анна' }\nparseData('не JSON');           // null",
        explanation: "Функция безопасно обрабатывает ошибку и возвращает null вместо падения."
      },
      {
        level: "real",
        code: "function divide(a, b) {\n  if (typeof a !== 'number' || typeof b !== 'number') {\n    throw new TypeError('Аргументы должны быть числами');\n  }\n  if (b === 0) {\n    throw new Error('Деление на ноль');\n  }\n  return a / b;\n}\n\ntry {\n  console.log(divide(10, 2)); // 5\n  console.log(divide(10, 0)); // Ошибка!\n} catch (error) {\n  console.error(error.message); // 'Деление на ноль'\n}",
        explanation: "throw создаёт собственные ошибки. Это полезно для валидации входных данных."
      }
    ],
    commonMistakes: [
      {
        wrong: "Использовать try/catch для всех ошибок",
        why: "try/catch — для ожидаемых ошибок (невалидный JSON, сетевые запросы). Не для программных ошибок.",
        right: "Исправляйте ошибки в коде, а не перехватывайте их."
      },
      {
        wrong: "Забывать, что catch ловит только ошибки в try",
        why: "Ошибка в catch НЕ ловится этим же catch. Нужен вложеный try/catch.",
        right: "Каждый try/catch ловит ошибки только в своём try-блоке."
      },
      {
        wrong: "Пустой catch",
        why: "catch (error) {} — игнорирует ошибку. Проблема остаётся невидимой.",
        right: "Всегда логируйте ошибку: console.error(error)."
      }
    ],
    importantToRemember: [
      "try/catch перехватывает ошибки, не останавливая программу",
      "catch(error) — получает объект ошибки",
      "finally — выполняется всегда",
      "throw new Error() — создание собственных ошибок",
      "Используйте для ожидаемых ошибок, не для программных"
    ],
    connection: {
      back: "Вы знаете методы строк и чисел (J21-J22) — теперь вы умеете обрабатывать их сбои.",
      forward: "Следующий урок (J24) — пользовательские ошибки: создание собственных типов ошибок."
    }
  },

  // ============================================
  // J24 — Custom Errors
  // ============================================
  {
    slug: "custom-errors",
    track: "js-core",
    order: 25,
    title: "Пользовательские ошибки",
    summary: "Научиться создавать собственные ошибки с помощью throw new Error и пользовательских классов ошибок для более точной обработки.",
    level: "Beginner",
    prerequisites: ["try-catch"],
    learningObjective: "После этого урока вы сможете создавать собственные классы ошибок, выбрасывать осмысленные ошибки и обрабатывать разные типы ошибок по-разному.",
    shortExplanation: "Помимо встроенных ошибок (TypeError, ReferenceError), вы можете создавать собственные ошибки: throw new Error('описание'). Для сложных случаев — пользовательские классы ошибок: class ValidationError extends Error.",
    detailedExplanation: "Обратите внимание: для пользовательских ошибок ниже используется синтаксис class и extends — он детально разбирается в разделе JS Intermediate (классы, JI8). Пока воспринимайте этот код как образец, главное здесь — идея своих типов ошибок.\n\nСоздание ошибки:\nthrow new Error('Что-то пошло не так');\nthrow new TypeError('Ожидается число');\nthrow new RangeError('Число должно быть от 1 до 10');\n\nПользовательские ошибки:\nclass ValidationError extends Error {\n  constructor(message, field) {\n    super(message);\n    this.name = 'ValidationError';\n    this.field = field;\n  }\n}\n\nИспользование:\nfunction validateAge(age) {\n  if (age < 0) {\n    throw new ValidationError('Возраст не может быть отрицательным', 'age');\n  }\n}\n\nОбработка разных ошибок:\ntry {\n  validateAge(-5);\n} catch (error) {\n  if (error instanceof ValidationError) {\n    console.error(`Ошибка в поле ${error.field}: ${error.message}`);\n  } else {\n    console.error('Неожиданная ошибка:', error);\n  }\n}\n\nЗачем собственные ошибки:\n- Разные типы ошибок → разная обработка\n- Дополнительная информация (field, code)\n- Читаемый код (ValidationError вместо Error)",
    mentalModel: "Пользовательские ошибки — как маркированные пакеты. Вместо одного ящика «ошибки» — отдельные ящики «ошибка валидации», «ошибка сети», «ошибка авторизации». Каждый обрабатывается по-своему.",
    examples: [
      {
        level: "minimal",
        code: "function divide(a, b) {\n  if (b === 0) {\n    throw new Error('Деление на ноль невозможно');\n  }\n  return a / b;\n}\n\ntry {\n  divide(10, 0);\n} catch (error) {\n  console.error(error.message); // 'Деление на ноль невозможно'\n}",
        explanation: "throw создаёт ошибку с описанием. catch ловит её."
      },
      {
        level: "simple",
        code: "class NotFoundError extends Error {\n  constructor(resource, id) {\n    super(`${resource} with id ${id} not found`);\n    this.name = 'NotFoundError';\n    this.resource = resource;\n    this.id = id;\n  }\n}\n\nfunction findUser(id) {\n  const users = { 1: 'Анна' };\n  if (!users[id]) throw new NotFoundError('User', id);\n  return users[id];\n}",
        explanation: "Пользовательская ошибка с дополнительной информацией: resource и id. Синтаксис класса здесь — пример будущей темы (классы, JS Intermediate)."
      },
      {
        level: "real",
        code: "class AppError extends Error {\n  constructor(message, code, statusCode) {\n    super(message);\n    this.name = 'AppError';\n    this.code = code;\n    this.statusCode = statusCode;\n  }\n}\n\nclass ValidationError extends AppError {\n  constructor(field, message) {\n    super(message, 'VALIDATION_ERROR', 400);\n    this.name = 'ValidationError';\n    this.field = field;\n  }\n}\n\nclass NotFoundError extends AppError {\n  constructor(resource) {\n    super(`${resource} not found`, 'NOT_FOUND', 404);\n    this.name = 'NotFoundError';\n  }\n}",
        explanation: "Иерархия ошибок: базовый AppError → ValidationError, NotFoundError. Каждая с своим кодом и HTTP-статусом. Это пример будущей темы: наследование классов (class ... extends) полностью разбирается в JS Intermediate (JI8–JI9)."
      }
    ],
    commonMistakes: [
      {
        wrong: "Бросать строки вместо объектов Error",
        why: "throw 'ошибка' — работает, но не содержит стек-трейс и не instanceof Error.",
        right: "Всегда: throw new Error('описание')."
      },
      {
        wrong: "Создавать слишком много типов ошибок",
        why: "Если обработка одинаковая — достаточно одного типа. Не усложняйте.",
        right: "Создавайте собственные ошибки только когда нужна разная обработка."
      },
      {
        wrong: "Забывать extends Error",
        why: "Без extends Error ваш класс не будет распознан как ошибка.",
        right: "Ваш класс ошибки должен расширять Error для правильного наследования."
      }
    ],
    importantToRemember: [
      "throw new Error('message') — создание ошибки",
      "extends Error — пользовательская ошибка",
      "instanceof — проверка типа ошибки",
      "Разные ошибки → разная обработка",
      "Всегда бросайте объекты Error, не строки"
    ],
    connection: {
      back: "Вы знаете try/catch (J23) — теперь вы умеете создавать осмысленные ошибки.",
      forward: "Следующий урок (J25) — модули: организация кода по файлам."
    }
  },

  // ============================================
  // J25 — Modules
  // ============================================
  {
    slug: "modules",
    track: "js-core",
    order: 26,
    title: "Модули (ES Modules)",
    summary: "Научиться разделять код на файлы-модули с помощью import/export, понимая named и default exports, модульную область видимости и type=\"module\".",
    level: "Beginner",
    prerequisites: ["custom-errors"],
    learningObjective: "После этого урока вы сможете создавать и использовать ES-модули с import/export, объяснять область видимости модуля и понимать роль type=\"module\".",
    shortExplanation: "Модули — способ разделять код на файлы. export делает функцию/переменную доступной для других файлов. import подключает их. Каждый модуль имеет свою область видимости — переменные не «вытекают» наружу.",
    detailedExplanation: "Зачем модули:\n- Без модулей весь код в одном файле (или <script> тегах)\n- Модули позволяют разделять логику по файлам\n- Каждый модуль — изолированная область видимости\n- Не конфликтуют имена переменных\n\nExport:\n// math.js\nexport function add(a, b) { return a + b; }\nexport const PI = 3.14159;\n\nImport:\n// app.js\nimport { add, PI } from './math.js';\nconsole.log(add(2, 3)); // 5\n\nNamed export vs Default export:\n// Named: export { add, PI };\n// Default: export default function greet() {}\n\n// Импорт default:\nimport greet from './greet.js';\n// Без фигурных скобок!\n\nModule scope:\n// В модуле переменные изолированы:\n// module-a.js\nconst secret = 42; // не доступен другим модулям\nexport function getSecret() { return secret; }\n\nHTML:\n<script type=\"module\" src=\"app.js\"></script>\n\ntype=\"module\" обязательно:\n- Без него браузер не понимает import/export\n- Модули загружаются с defer по умолчанию\n- Модули имеют строгий режим по умолчанию",
    mentalModel: "Модули — как библиотечные книги. Каждая книга (модуль) имеет своё содержимое (переменные). Вы берёте из библиотеки (import) только то, что нужно. Если книга не «экспортирует» информацию — она доступна только внутри.",
    examples: [
      {
        level: "minimal",
        code: "// math.js\nexport function add(a, b) {\n  return a + b;\n}\n\n// app.js\nimport { add } from './math.js';\nconsole.log(add(2, 3)); // 5",
        explanation: "Самый простой модуль: экспорт функции и импорт в другом файле."
      },
      {
        level: "simple",
        code: "// utils.js\nexport const PI = 3.14159;\nexport function circleArea(r) { return PI * r * r; }\nexport default function greet(name) { return `Hi, ${name}`; }\n\n// app.js\nimport greet, { PI, circleArea } from './utils.js';\n//         ^default  ^named\n\nconsole.log(greet('Анна'));    // 'Hi, Анна'\nconsole.log(circleArea(5));    // 78.53975",
        explanation: "Named export (фигурные скобки) и default export (без скобок) можно комбинировать."
      },
      {
        level: "real",
        code: "// api.js\nlet baseUrl = '';\n\nexport function setBaseUrl(url) {\n  baseUrl = url;\n}\n\nexport async function fetchData(endpoint) {\n  const response = await fetch(baseUrl + endpoint);\n  return response.json();\n}\n\n// app.js\nimport { setBaseUrl, fetchData } from './api.js';\n\nsetBaseUrl('https://api.example.com');\nconst data = await fetchData('/users');",
        explanation: "Реальный пример: модуль API с приватной переменной baseUrl и публичными функциями."
      }
    ],
    commonMistakes: [
      {
        wrong: "Забывать type=\"module\" в HTML",
        why: "Без type=\"module\" браузер игнорирует import/export и выбрасывает ошибку.",
        right: "<script type=\"module\" src=\"app.js\"></script>"
      },
      {
        wrong: "Путать named и default export",
        why: "Named export: import { name } from './module.js'. Default: import name from './module.js'.",
        right: "Default — без фигурных скобок. Named — с фигурными скобками."
      },
      {
        wrong: "Думать, что модули — глобальные",
        why: "Модули изолированы. Переменная в модуле A не видна в модуле B без export/import.",
        right: "Модули изолированы. Используйте export/import для общения между ними."
      }
    ],
    importantToRemember: [
      "export — делает доступным для других модулей",
      "import { name } from './module.js' — подключает",
      "export default — экспорт по умолчанию (без имени)",
      "type=\"module\" обязательно в HTML",
      "Каждый модуль — изолированная область видимости"
    ],
    connection: {
      back: "Вы знаете функции и объекты (J9-J18) — теперь вы можете организовать их по файлам.",
      forward: "Следующий урок (J26) — стиль кода: написание чистого и читаемого JavaScript."
    }
  },

  // ============================================
  // J26 — Code Style
  // ============================================
  {
    slug: "code-style",
    track: "js-core",
    order: 27,
    title: "Стиль кода",
    summary: "Научиться писать чистый, читаемый JavaScript: именование переменных, форматирование, паттерны, которые делают код понятным другим разработчикам.",
    level: "Beginner",
    prerequisites: ["modules"],
    learningObjective: "После этого урока вы сможете писать чистый JavaScript-код по современным стандартам стиля и объяснять, почему стиль кода важен.",
    shortExplanation: "Стиль кода — это правила оформления, которые делают код читаемым. Хороший код понимают другие разработчики (и вы через неделю). Основные правила: camelCase, осмысленные имена, консистентность, комментарии к сложной логике.",
    detailedExplanation: "Почему стиль важен:\n- Код читают в 10 раз чаще, чем пишут\n- Нечитаемый код — источник ошибок\n- Командная работа требует единых стандартов\n\nИменование:\n- camelCase для переменных и функций: userName, getUser()\n- PascalCase для классов: User, ValidationError\n- UPPER_SNAKE_CASE для констант: MAX_RETRIES\n- Булевы: is/has/can: isActive, hasPermission\n- Массивы: во множественном: items, users\n\nФорматирование:\n- 2 пробела (или 4 — выберите и придерживайтесь)\n- ; в конце инструкций\n- Пробелы вокруг операторов: let x = 5 + 3;\n- Пустая строка между блоками кода\n\nПаттерны:\n- const по умолчанию, let если нужно менять\n- Ранний return (guard clause)\n- Destructuring для параметров\n- Template literals для строк\n\nПлохо vs Хорошо:\nПлохо: let d = new Date(); let x = d.getHours(); if (x > 12) {...}\nХорошо: const currentHour = new Date().getHours();\n        if (currentHour > 12) { ... }",
    mentalModel: "Стиль кода — как пунктуация в тексте. Без неё текст читаем, но с трудом. Со стилем — понимаем сразу.",
    examples: [
      {
        level: "minimal",
        code: "// Плохо:\nlet x = 10;\nlet y = 20;\nlet z = x + y;\n\n// Хорошо:\nconst firstNumber = 10;\nconst secondNumber = 20;\nconst sum = firstNumber + secondNumber;",
        explanation: "Осмысленные имена переменных делают код самодокументируемым."
      },
      {
        level: "simple",
        code: "// Плохо:\nfunction f(a, b) {\n  if (a > b) return a;\n  else return b;\n}\n\n// Хорошо:\nfunction getMax(first, second) {\n  return first > second ? first : second;\n}\n// Или ещё лучше: Math.max(first, second)",
        explanation: "Имя функции должно говорить, ЧТО она делает."
      },
      {
        level: "real",
        code: "// Плохо:\nfunction p(u) {\n  if (u.a && u.b) {\n    return u.a + u.b;\n  }\n  return 0;\n}\n\n// Хорошо:\nfunction calculateTotalScore(user) {\n  if (!user.firstName || !user.lastName) {\n    return 0;\n  }\n  return user.firstName + user.lastName;\n}\n\n// Ещё лучше:\nfunction calculateTotalScore({ firstName = '', lastName = '' } = {}) {\n  return firstName + lastName;\n}",
        explanation: "Destructuring параметров + значения по умолчанию + осмысленные имена."
      }
    ],
    commonMistakes: [
      {
        wrong: "Использовать однострочные имена для важных переменных",
        why: "x, y, z, d, s — ничего не говорят о назначении переменной.",
        right: "Используйте осмысленные имена: userName, totalScore, currentDate."
      },
      {
        wrong: "Миксовать стили в одном проекте",
        why: "Когда каждый пишет по-своему — код нечитаем.",
        right: "Согласуйте стиль и придерживайтесь его. Используйте линтеры (ESLint)."
      },
      {
        wrong: "Комментировать каждый день",
        why: "Комментарии для «ЗАЧЁМ», а не «ЧТО». Если код понятен из имён — комментарий не нужен.",
        right: "Комментируйте сложную логику, не очевидный код."
      }
    ],
    importantToRemember: [
      "camelCase для переменных и функций",
      "PascalCase для классов",
      "Осмысленные имена > короткие имена",
      "const по умолчанию, let если нужно",
      "Комментируйте ЗАЧЁМ, а не ЧТО"
    ],
    connection: {
      back: "Теперь вы знаете все концепции JavaScript Core (J0-J26) — этот урок связывает их воедино.",
      forward: "Вы завершили JavaScript Core! Дальше — DOM Basics (D1-D8): как связать JavaScript с веб-страницей."
    }
  }
] as const;
